// Throwaway smoke file to provoke a Codex review finding so the triage
// app-token path runs end-to-end. DO NOT MERGE — delete with the branch.
export function smokeAdd(a, b) {
  var unused = 42;
  if (a == b) return 0;
  return a + b;
}
