---
phase: 17-cookie-consent-product-schema
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/products/[handle]/page.tsx
autonomous: true
requirements:
  - SEO-01

must_haves:
  truths:
    - "Product detail pages include JSON-LD Product schema structured data."
    - "The schema uses real data pulled from the product object."
  artifacts:
    - path: "app/products/[handle]/page.tsx"
      provides: "Injects <script type=\"application/ld+json\"> schema block."
      contains: "application/ld+json"
  key_links: []
---

<objective>
Inject JSON-LD Product schema structured data into product detail pages.
</objective>

<execution_context>
@/Users/jamesputman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesputman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject Product Schema into product page</name>
  <files>app/products/[handle]/page.tsx</files>
  <action>
    In `app/products/[handle]/page.tsx`, within the returned JSX, dynamically render a script tag for JSON-LD.
    
    ```tsx
    const productJsonLd = {
      '@context': 'https://schema.org',
      '@type': 'Product',
      name: product.title,
      description: product.description,
      image: product.featuredImage?.url,
      offers: {
        '@type': 'AggregateOffer',
        availability: product.availableForSale
          ? 'https://schema.org/InStock'
          : 'https://schema.org/OutOfStock',
        priceCurrency: product.priceRange.minVariantPrice.currencyCode,
        highPrice: product.priceRange.maxVariantPrice.amount,
        lowPrice: product.priceRange.minVariantPrice.amount,
      },
      brand: {
        '@type': 'Brand',
        name: 'Wildenflower'
      }
    };
    ```
    Render it optimally inside the React tree:
    ```tsx
    <script
      type="application/ld+json"
      dangerouslySetInnerHTML={{ __html: JSON.stringify(productJsonLd) }}
    />
    ```
    Insert it inside the `<div className="mx-auto max-w-7xl...">` main wrapper or similar safe location.
  </action>
  <verify>
    <automated>grep -c 'application/ld+json' app/products/\[handle\]/page.tsx</automated>
  </verify>
  <done>Product schema generated dynamically via JSON-LD.</done>
</task>

</tasks>

<verification>
After all tasks:
1. `npm run build` completes without errors
2. The product page contains `application/ld+json` script tag with aggregate offer data.
</verification>

<success_criteria>
SEO structured data for products exists and is fully compliant with Shopify `Product` object.
</success_criteria>

<output>
After completion, create `.planning/phases/17-cookie-consent-product-schema/17-02-SUMMARY.md`
</output>
