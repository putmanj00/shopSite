---
phase: 17-cookie-consent-product-schema
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ui/cookie-banner.tsx
  - app/layout.tsx
autonomous: true
requirements:
  - GDPR-01
  - GDPR-02

must_haves:
  truths:
    - "A cookie consent banner appears on first visit."
    - "The banner includes accept/reject options and links to the Privacy Policy."
    - "Consent is persisted in localStorage to prevent reappearance."
  artifacts:
    - path: "components/ui/cookie-banner.tsx"
      provides: "Client component for the cookie banner."
      contains: "localStorage"
    - path: "app/layout.tsx"
      provides: "Include CookieBanner in the root layout."
      contains: "<CookieBanner />"
  key_links:
    - from: "app/layout.tsx"
      to: "components/ui/cookie-banner.tsx"
      via: "import { CookieBanner } from '@/components/ui/cookie-banner'"
      pattern: "<CookieBanner />"
---

<objective>
Implement a persistent cookie consent banner to comply with GDPR-01 and GDPR-02.
</objective>

<execution_context>
@/Users/jamesputman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesputman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
```typescript
// components/ui/cookie-banner.tsx
"use client"
import { useState, useEffect } from 'react';
import Link from 'next/link';

export function CookieBanner() {
  const [show, setShow] = useState(false);

  useEffect(() => {
    const consent = localStorage.getItem('wildenflower_cookie_consent');
    if (!consent) setShow(true);
  }, []);

  const handleConsent = (choice: 'accepted' | 'rejected') => {
    localStorage.setItem('wildenflower_cookie_consent', choice);
    setShow(false);
  };

  if (!show) return null;

  return (
    <div className="fixed bottom-0 left-0 right-0 z-50 p-4 bg-[#F5EDD6] border-t border-[#8B7355] text-[#3e2723] shadow-lg">
      <div className="max-w-7xl mx-auto flex flex-col sm:flex-row items-center justify-between gap-4">
        <p className="text-sm">
          We use cookies to improve your experience. See our <Link href="/legal/privacy-policy" className="underline font-semibold hover:text-[#2d5a27]">Privacy Policy</Link> for details.
        </p>
        <div className="flex gap-4">
          <button onClick={() => handleConsent('rejected')} className="text-sm px-4 py-2 border border-[#8B7355] rounded hover:bg-[#eae0c8] transition-colors">
            Refuse
          </button>
          <button onClick={() => handleConsent('accepted')} className="text-sm px-6 py-2 bg-[#1E3B30] text-white rounded hover:bg-[#2d5a27] transition-colors">
            Accept
          </button>
        </div>
      </div>
    </div>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CookieBanner component</name>
  <files>components/ui/cookie-banner.tsx</files>
  <action>
    Create exact `CookieBanner` component as designed in interfaces block above. Must use `"use client"`.
  </action>
  <verify>
    <automated>test -f components/ui/cookie-banner.tsx && grep -c '"use client"' components/ui/cookie-banner.tsx</automated>
  </verify>
  <done>CookieBanner component created and uses localStorage for state.</done>
</task>

<task type="auto">
  <name>Task 2: Inject CookieBanner into layout</name>
  <files>app/layout.tsx</files>
  <action>
    Import and render `<CookieBanner />` inside the `<body>` of `app/layout.tsx`, just before the closing tag if possible, or alongside the `Footer`.
  </action>
  <verify>
    <automated>grep -c 'CookieBanner' app/layout.tsx</automated>
  </verify>
  <done>CookieBanner integrated into root layout.</done>
</task>

</tasks>

<verification>
After all tasks:
1. `npm run build` completes without errors
2. The site contains a CookieBanner in the root layout HTML hierarchy.
</verification>

<success_criteria>
Cookie banner displays on first visit and saves preference to localStorage.
</success_criteria>

<output>
After completion, create `.planning/phases/17-cookie-consent-product-schema/17-01-SUMMARY.md`
</output>
