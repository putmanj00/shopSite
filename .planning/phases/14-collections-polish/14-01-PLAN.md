---
phase: 14-collections-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "app/collections/[handle]/page.tsx"
autonomous: true
requirements:
  - COLL-01
  - COLL-02
---

<objective>
Update the Collection Title and add a Subtitle for the "all" collection handle.

Purpose: 
When visitors land on `/collections/all`, they should see brand-aligned copy instead of generic store copy like "Products".

Target: `app/collections/[handle]/page.tsx`
Output: Modified page component that conditionally renders "All Treasures" and "Every handmade treasure in one place" if the collection handle is "all".
</objective>

<execution_context>
@/Users/jamesputman/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Conditionally Update Heading</name>
  <files>
    - app/collections/[handle]/page.tsx
  </files>
  <action>
Modify the collection heading logic:
- If `params.handle === 'all'`, display "All Treasures" instead of evaluating `collection?.title` as "Products".
- Include a conditional check to render a subtitle: `Every handmade treasure in one place` immediately below the title, if `params.handle === 'all'`. Keep the typography matching the standard collection title setup (likely `font-playfair` and `text-earth` for title, maybe `font-lora` and `text-inkBrown/80` for subtitle).
  </action>
</task>

</tasks>

<success_criteria>
`app/collections/[handle]/page.tsx` conditionally uses "All Treasures" and "Every handmade treasure in one place" for the 'all' collection.
</success_criteria>
