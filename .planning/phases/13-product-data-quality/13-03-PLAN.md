---
phase: 13-product-data-quality
plan: 03
type: execute
wave: 3
depends_on:
  - 13-02
files_modified: []
autonomous: false
requirements:
  - PRDS-01
  - PRDS-02
  - PRDS-03
  - PRDS-04

must_haves:
  truths:
    - "Vendor 'My Store' is replaced by 'Wildenflower' in product cards, detail pages, and quick view"
    - "Test products 'ring' and 'Generic Tiedye' are absent from all product grids"
    - "No broken grey boxes or 'No image' text visible in any product grid"
    - "Botanical corner overlays visible on product cards — top-left and bottom-right corners"
    - "All four PRDS requirements confirmed by human visual inspection"
  artifacts:
    - path: "lib/product-filters.ts"
      provides: "Complete helper module used across all product surfaces"
    - path: "components/product-card.tsx"
      provides: "Card with vendor normalization + corner overlays + no broken image fallback"
  key_links:
    - from: "All product grids"
      to: "lib/product-filters.ts"
      via: "isShowableProduct filter + normalizeVendor display"
      pattern: "isShowableProduct|normalizeVendor"
---

<objective>
Human visual verification of all four PRDS requirements. Claude confirms the TypeScript build is clean, then pauses for the user to verify the changes in the browser.

Purpose: Confirm the product data quality changes work correctly in the actual rendered UI before Phase 13 is declared complete.
Output: User approval or issue report for any failing requirement.
</objective>

<execution_context>
@/Users/jamesputman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesputman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-product-data-quality/13-01-SUMMARY.md
@.planning/phases/13-product-data-quality/13-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm TypeScript build is clean before verification</name>
  <files></files>
  <action>
Run a clean TypeScript check to confirm no compilation errors introduced by Plans 01 and 02:

```bash
npx tsc --noEmit
```

If errors found, fix them before proceeding to the checkpoint.

Then confirm the dev server is running (or start it if not already running). The user will need http://localhost:3000 accessible to complete visual verification.
  </action>
  <verify>npx tsc --noEmit exits with code 0 (no output = clean)</verify>
  <done>TypeScript compiles cleanly; dev server accessible at http://localhost:3000</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification — all four PRDS requirements</name>
  <files></files>
  <action>Pause for human visual verification of all four PRDS requirements in the browser at http://localhost:3000</action>
  <what-built>
    Four product data quality fixes across the entire storefront:
    - PRDS-01: Vendor "My Store" → "Wildenflower" in product cards, detail pages, and quick view modals
    - PRDS-02: Test products ("ring", "Generic Tiedye") filtered from all product grids
    - PRDS-03: Products with no images filtered from all product grids — no grey boxes or "No image" text
    - PRDS-04: Botanical corner PNG overlays on product card images (top-left + bottom-right)
  </what-built>
  <how-to-verify>
With http://localhost:3000 open in your browser, verify each requirement:

**PRDS-01 — Vendor display:**
1. Visit http://localhost:3000 (homepage) — scroll to the "Freshly Gathered" product grid
2. Find any product card — the vendor line below the product title should read "Wildenflower", NOT "My Store"
3. Click any product card to open the detail page — the "by [vendor]" line below the product title should read "by Wildenflower"
4. Return to homepage — hover over any product card and click "Quick View" — the vendor line at the top of the modal should read "Wildenflower"

**PRDS-02 — Test product hiding:**
5. Scan the homepage product grid — no card titled "ring" (exact, lowercase) or "Generic Tiedye" should appear
6. Visit http://localhost:3000/collections/all — scan the grid for those titles; neither should appear

**PRDS-03 — No broken image states:**
7. Scan the homepage product grid — every card should show a product photo; no grey box, no "No image" text
8. Visit http://localhost:3000/collections/all — same check; every card should show a product photo

**PRDS-04 — Botanical corner overlays:**
9. On any product card (homepage or collections), look at the product image — you should see a small botanical corner decoration at the top-left and another at the bottom-right of the image
10. Hover over a product card — verify the Quick View button still appears on hover and is clickable
11. Click the Wishlist (heart) button on a card — verify it still responds (toggles)
  </how-to-verify>
  <verify>User confirms "approved" in chat</verify>
  <done>All four PRDS requirements pass human visual inspection</done>
  <resume-signal>Type "approved" to complete Phase 13, or describe any issues found (e.g., "PRDS-01 still shows My Store on detail page")</resume-signal>
</task>

</tasks>

<verification>
All four PRDS requirements visually confirmed by user:
- PRDS-01: "Wildenflower" appears on card, detail page, and quick view — not "My Store"
- PRDS-02: No test/placeholder products in any grid
- PRDS-03: No broken image states in any grid
- PRDS-04: Botanical corner overlays visible on cards; interactive elements (wishlist, quick view) unaffected
</verification>

<success_criteria>
User types "approved" confirming all four PRDS requirements pass visual inspection.
</success_criteria>

<output>
After completion, create .planning/phases/13-product-data-quality/13-03-SUMMARY.md
</output>
