---
phase: 10-trust-cleanup
plan: 04
type: execute
wave: 2
depends_on:
  - "10-01"
  - "10-02"
  - "10-03"
files_modified: []
autonomous: false
requirements:
  - TRST-01
  - TRST-02
  - TRST-03
  - TRST-04

must_haves:
  truths:
    - "No fake purchase popup appears on any page visit"
    - "No fake testimonials or stock-photo reviewer personas appear on the homepage"
    - "No fabricated stats appear anywhere on the site"
    - "No fake Instagram engagement counts or stock gallery photos appear on the homepage"
    - "Welcome popup fires at 15s or 50% scroll — not immediately"
    - "Welcome popup shows botanical image and Wildenflower Inner Circle copy"
    - "Find Us in the Wild events section appears in the testimonial slot with real market data"
  artifacts:
    - path: "app/layout.tsx"
      provides: "No SocialProofToast or RecentPurchasePopup in imports or JSX"
    - path: "components/homepage/instagram-gallery.tsx"
      provides: "Returns null — no gallery renders"
    - path: "components/homepage/find-us-in-the-wild.tsx"
      provides: "Events section live on homepage"
    - path: "components/cro/welcome-popup.tsx"
      provides: "Updated copy, image, timing"
    - path: "app/sustainability/page.tsx"
      provides: "No fabricated Our Impact stats"
  key_links: []
---

<objective>
Human visual verification of all Phase 10 trust cleanup changes. Confirm every fake social proof element is gone and replacements look correct.

Purpose: User approves the result before Phase 10 is marked complete and Phase 11 (Navigation Routing) begins.
Output: User-approved Phase 10 — all four TRST requirements verified by visual inspection.
</objective>

<execution_context>
@/Users/jamesputman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesputman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-trust-cleanup/10-01-SUMMARY.md
@.planning/phases/10-trust-cleanup/10-02-SUMMARY.md
@.planning/phases/10-trust-cleanup/10-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Visual verification: all four TRST requirements</name>
  <action>Start the dev server and walk through each check below. This task has no automated implementation — it confirms what plans 10-01, 10-02, and 10-03 built.</action>
  <what-built>
    Plans 10-01 through 10-03 completed the following trust cleanup:

    - **TRST-01**: Removed SocialProofToast (fake "SOLD" toast) and RecentPurchasePopup (fake "someone just bought" notification) from app/layout.tsx — neither renders anymore
    - **TRST-02**: Replaced fake TestimonialCarousel (Sarah M., Michael R., Emily L. personas) with FindUsInTheWild events section showing real Wildenflower markets
    - **TRST-03**: Removed fabricated stats from testimonial-carousel (4.9 rating, 98% recommend) and from sustainability page (200+ Artisan Partners, 100% Carbon Neutral Shipping, 0 Single-Use Plastics, 15 Countries Represented)
    - **TRST-04**: Stubbed instagram-gallery.tsx to return null — no more Unsplash photos with fake like counts
    - **Bonus**: Updated welcome-popup.tsx with Wildenflower Inner Circle copy, botanical image, and 15s/50%-scroll delayed trigger
  </what-built>
  <how-to-verify>
    Start the dev server: `cd /Users/jamesputman/SRC/shopSite && npm run dev`

    **Check 1 — Homepage (http://localhost:3000):**
    1. Wait for full page load. Scroll slowly through the page.
    2. Confirm NO purchase notification popup appears in the bottom-left corner (the "SOLD" badge toast) after 5-10 seconds. Wait at least 15 seconds.
    3. Confirm NO "Someone just bought X" modal popup appears anywhere on the page.
    4. Scroll past the TrustBar section — confirm you see a "Find Us in the Wild" events section with market cards (Covington, Cincinnati) — NOT fake reviewer cards with star ratings.
    5. Confirm NO Instagram gallery with photos and like-count overlays appears below the events section. That section should be absent entirely.

    **Check 2 — Welcome popup timing (http://localhost:3000 in private/incognito window):**
    1. Open a new incognito window to clear localStorage.
    2. Visit http://localhost:3000.
    3. DO NOT SCROLL. Wait 15 seconds — the welcome popup should appear automatically.
    4. Confirm: headline reads "Join the Wildenflower Inner Circle"; body mentions hand-dyed drops, leatherwork, rare mineral finds, Covington/Cincy markets; CTA button reads "Claim My Welcome Discount"; dismiss link reads "Maybe later".
    5. Confirm: the left panel (desktop) shows a botanical image (green botanical header), NOT a white marble counter / POS terminal photo.
    6. Dismiss the popup. Refresh the page — popup should NOT appear again (localStorage flag).

    **Check 3 — Sustainability page (http://localhost:3000/sustainability):**
    1. Scroll through the sustainability page.
    2. Confirm NO "Our Impact" section with "200+ Artisan Partners", "100% Carbon Neutral Shipping", "0 Single-Use Plastics", "15 Countries Represented" stats appears. The page should flow from the practices grid directly to the "Sustainability Milestones" timeline.

    **Check 4 — Overall page flow:**
    1. On the homepage, confirm page flows naturally: Hero → Categories → Products → Brand Story → TrustBar → Find Us in the Wild events → Newsletter. No jarring empty gaps.
  </how-to-verify>
  <verify>Human visual inspection per steps above — dev server must be running</verify>
  <done>User types "approved" — all four TRST requirements visually confirmed clean</done>
  <resume-signal>Type "approved" to mark Phase 10 complete. Or describe any issues found so they can be fixed.</resume-signal>
</task>

</tasks>

<verification>
All verification is human-performed above. Once approved, run a final TypeScript check:
`cd /Users/jamesputman/SRC/shopSite && npx tsc --noEmit 2>&1 | head -20`
</verification>

<success_criteria>
- All four TRST requirements visually verified by user
- No fake social proof visible on any checked page
- Welcome popup fires at delayed timing (not immediately), shows correct copy and botanical image
- Find Us in the Wild section visible and styled correctly
- User types "approved"
</success_criteria>

<output>
After approval, create `.planning/phases/10-trust-cleanup/10-04-SUMMARY.md` following the summary template.

Then update ROADMAP.md Phase 10 status to Complete and update STATE.md current position to Phase 11.
</output>
