---
phase: 15-footer-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "components/footer.tsx"
autonomous: true
requirements:
  - FOOT-01
  - FOOT-02
---

<objective>
Correct the SHOP navigation column in the footer to match the exact six categories + the All Treasures link, and remove links to empty/non-existent pages (Size Guide, Our Story, Sustainability, Accessibility, Press, etc).

Target: `components/footer.tsx`
Output: A fully populated and accurate footer corresponding to our established routing scheme.
</objective>

<execution_context>
@/Users/jamesputman/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Footer Links Structure</name>
  <files>
    - components/footer.tsx
  </files>
  <action>
In `components/footer.tsx`, update `footerLinks.shop` to match:
  1. "All Treasures" -> `/collections/all`
  2. "Tie-Dye" -> `/collections/tie-dye`
  3. "Leather" -> `/collections/leather`
  4. "Jewelry" -> `/collections/jewelry`
  5. "Crystals" -> `/collections/crystals`
  6. "Artwork" -> `/collections/artwork`
  7. "Ceramics" -> `/collections/ceramics`

In `footerLinks.support`, modify to ensure it only has links we actually have pages for, e.g. (`/contact`, `/shipping-returns`, `/faq`). **Remove `Size Guide` and `Press`** if they are present or not found in `app`.
In `footerLinks.company`, verify the presence of `Our Story` (`/about` mapping normally) and `Blog`. **Remove `Sustainability`** since that page is removed/incomplete according to PRDS tracking and PRs.
In `footerLinks.legal`, ensure `/privacy`, `/terms`, etc resolve. **Remove `Accessibility`** if that is deemed dead.
  </action>
</task>

</tasks>

<success_criteria>
`components/footer.tsx` defines precisely the valid working paths based on the app directory validation above.
</success_criteria>
