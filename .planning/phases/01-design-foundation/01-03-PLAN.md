---
phase: 01-design-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified: []
autonomous: false
requirements:
  - DESIGN-01
  - DESIGN-02
  - DESIGN-03
  - DESIGN-04
  - DESIGN-05

must_haves:
  truths:
    - "Every page background renders as parchment (#F5EDD6) — visible in dev server"
    - "Headings render in Playfair Display bold; body text renders in Lora regular — visible in dev server"
    - "Browser tab shows forest green theme color on mobile"
    - "Page title shows 'Wildenflower | Tie-Dye, Leather Goods, Jewelry & Art' — no psychedelic language"
    - "Dark mode does not activate — parchment remains on OS dark mode toggle"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of Phase 1 design foundation. The dev server is running; the user inspects that parchment background, botanical fonts, correct browser metadata, and dark mode suppression are working correctly across all pages.

Purpose: User must visually approve the foundation before Phase 2 (Header) begins. Per project preference, interactive approval is required at each phase boundary.
Output: User approval to proceed, or specific issues to fix.
</objective>

<execution_context>
@/Users/jamesputman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesputman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-design-foundation/01-01-SUMMARY.md
@.planning/phases/01-design-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Visual verification: botanical foundation visible in dev server</name>
  <action>
Human review of dev server. Start the dev server if not running: `npm run dev` in /Users/jamesputman/SRC/shopSite, then open http://localhost:3000.

What was built (Plans 01 + 02):
- Playfair Display + Lora fonts via next/font on &lt;html&gt; element; old Righteous/Nunito/Sacramento removed
- 8 botanical Tailwind color tokens in @theme inline (bg-parchment, bg-forest, text-ink-brown, bg-terracotta, etc.)
- Parchment (#F5EDD6) as default body background; inkBrown (#5C4033) as default text
- Dark mode suppressed (color-scheme: light on :root; both @media dark blocks removed)
- Updated metadata: "Wildenflower | Tie-Dye, Leather Goods, Jewelry & Art" title; botanical brand description
- Viewport themeColor and manifest.json both updated to forest green (#1E3B30)
- All dark: Tailwind classes removed from 4 component files
  </action>
  <verify>
Visually inspect in browser:

1. **Background**: Page background is warm parchment (#F5EDD6) — not grey, not white
2. **Text**: Body text is dark brown (inkBrown #5C4033)
3. **Headings**: h1/h2/h3 render in Playfair Display (elegant serif) — DevTools → Computed → font-family confirms
4. **Body font**: Paragraphs render in Lora (warm serif) — not Nunito rounded sans
5. **Browser tab**: Title reads "Wildenflower | Tie-Dye, Leather Goods, Jewelry & Art"
6. **Dark mode**: Toggle OS dark mode — parchment stays, no dark inversion
7. **Tokens** (optional): `getComputedStyle(document.documentElement).getPropertyValue('--color-parchment')` returns `#F5EDD6` in DevTools console
  </verify>
  <done>User types "approved" — all visual checks pass.</done>
  <resume-signal>Type "approved" if everything looks correct, or describe any issues (e.g., "fonts not loading", "background is still grey", "dark mode still activating")</resume-signal>
</task>

</tasks>

<verification>
This plan IS the verification. User visual approval is the gate.
</verification>

<success_criteria>
User types "approved" — Phase 1 Design Foundation is complete and Phase 2 (Header) can begin.
</success_criteria>

<output>
After user approval, create `.planning/phases/01-design-foundation/01-03-SUMMARY.md` with outcome notes, then update STATE.md:
- Phase 1 status: complete
- Current focus: Phase 2 — Header
</output>
