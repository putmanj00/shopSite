---
phase: 02-header
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/header.tsx
  - components/currency-selector.tsx
autonomous: false
requirements: [HEAD-01, HEAD-02]

must_haves:
  truths:
    - "Wildenflower logo-full.png renders in the header — no broken image or empty space"
    - "Header background is forest green (#1E3B30) — no neutral-50 grey visible"
    - "Nav links and Sign In text are parchment (#F5EDD6) with gold hover — no neutral-700 or purple visible"
    - "SVG icon buttons (cart, wishlist, search) are parchment-colored with white/10 hover backgrounds"
    - "Cart and wishlist count badges use terracotta — no purple (primary-500) or magenta (secondary-500)"
    - "CurrencySelector text is legible on the forest background (parchment, not gray-700)"
    - "All nav links, cart, wishlist, search, and auth integrations continue to work"
  artifacts:
    - path: "components/header.tsx"
      provides: "Header with botanical palette and correct logo"
      contains: "logo-full.png"
    - path: "components/currency-selector.tsx"
      provides: "CurrencySelector legible on forest background"
      contains: "text-parchment"
  key_links:
    - from: "components/header.tsx"
      to: "public/assets/images/logo/logo-full.png"
      via: "Next.js Image src prop"
      pattern: "assets/images/logo/logo-full\\.png"
    - from: "components/header.tsx"
      to: "globals.css @theme tokens"
      via: "Tailwind utility classes"
      pattern: "bg-forest|text-parchment|text-gold|border-gold|bg-terracotta"
---

<objective>
Apply the Wildenflower botanical palette to the site header — swap the broken logo path to logo-full.png and replace all psychedelic color classes with botanical equivalents in header.tsx and currency-selector.tsx.

Purpose: The header is the first branded element users see. Right now it shows a grey neutral background, neutral-700 text, and a broken logo image. This plan makes it read as Wildenflower: forest green with parchment text, the actual logo, and terracotta badges.

Output: Two files edited (header.tsx + currency-selector.tsx), visual verification checkpoint.
</objective>

<execution_context>
@/Users/jamesputman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesputman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-header/02-CONTEXT.md
@.planning/phases/02-header/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply botanical palette and logo to header.tsx and currency-selector.tsx</name>
  <files>components/header.tsx, components/currency-selector.tsx</files>
  <action>
Edit `components/header.tsx` with these exact changes — no structural, layout, or logic changes:

**Logo container (HEAD-01):**
- Change container: `className="relative h-10 w-56"` → `className="relative h-10 w-10"`
- Change src: `/images/wildenflower-full.png` → `/assets/images/logo/logo-full.png`
- Change image className: `"object-contain object-left"` → `"object-contain"`
  (logo-full.png is 600x600 square — remove object-left since the container is now square)

**Header element (HEAD-02):**
- `bg-neutral-50` → `bg-forest`
- `border-neutral-200` → `border-gold`

**Nav links (HEAD-02):**
- All instances of `text-neutral-700 hover:text-primary-600` → `text-parchment hover:text-gold`
  (This covers desktop nav links AND the mobile "Sign In" link — replace ALL occurrences)

**SVG icon buttons — cart, wishlist, search (HEAD-02):**
- All instances of `text-neutral-700` on SVG icons → `text-parchment`
- All instances of `hover:bg-neutral-100` on icon button wrappers → `hover:bg-white/10`

**Count badges (HEAD-02, Claude's discretion):**
- `bg-primary-500` (cart badge) → `bg-terracotta`
- `bg-secondary-500` (wishlist badge) → `bg-terracotta`
  Rationale: terracotta (#C8642A) reads as a warm alert color on forest green; gold is better for borders/accents than small notification dots.

**IMPORTANT — do NOT touch:**
- `sticky top-0 z-30` (structural layout classes)
- `isMounted` guards on badge visibility (prevents hydration mismatch)
- Any Shopify cart/auth/wishlist event handlers or state
- Mobile menu open/close logic
- All href values on nav links

---

Edit `components/currency-selector.tsx` with these exact changes (HEAD-02):

**Trigger button text (visible on forest background):**
- `text-gray-700` → `text-parchment`
- `hover:text-gray-900` → `hover:text-white`
- `hover:bg-gray-100` → `hover:bg-white/10`

**Chevron icon (currently near-invisible gray on forest):**
- `text-gray-400` → `text-parchment/60`

**Dropdown panel:** Leave as-is — it floats above the header on a white background; dark text on white is correct.

**IMPORTANT — do NOT touch:** dropdown open/close logic, currency state, onSelect handlers, or any option rendering logic.
  </action>
  <verify>
    <automated>cd /Users/jamesputman/SRC/shopSite && grep -n "logo-full.png\|bg-forest\|text-parchment\|border-gold\|bg-terracotta\|hover:bg-white/10" components/header.tsx | head -20</automated>
    <manual>Confirm output shows logo-full.png path, bg-forest on header, text-parchment on links, border-gold, bg-terracotta on badges, and hover:bg-white/10 on icon buttons.</manual>
  </verify>
  <done>
    header.tsx: logo src is `/assets/images/logo/logo-full.png`, container is `h-10 w-10`, header has `bg-forest border-gold`, nav links are `text-parchment hover:text-gold`, icon buttons are `text-parchment hover:bg-white/10`, badges are `bg-terracotta`.
    currency-selector.tsx: trigger is `text-parchment hover:text-white hover:bg-white/10`, chevron is `text-parchment/60`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification — header is botanically branded</name>
  <action>Pause for human visual verification of the header changes. Dev server must be running at http://localhost:3000.</action>
  <what-built>
    Botanical palette applied to header: forest green background, parchment nav text, gold border, Wildenflower logo, terracotta badges, and legible CurrencySelector — all in header.tsx and currency-selector.tsx.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` if not already running (http://localhost:3000)
    2. Open http://localhost:3000 in browser
    3. Inspect the sticky nav bar at the top:
       - Background is FOREST GREEN (dark green, not grey or white)
       - Bottom border is a thin GOLD line
       - WILDENFLOWER LOGO is visible (square logo mark, not a broken image icon)
       - Nav link text is PARCHMENT (warm off-white/cream), not dark grey
       - Hover over a nav link — text turns GOLD
       - Currency selector text (e.g., "USD") is readable on the green background
    4. Check icon buttons (cart, wishlist, search):
       - Icons are parchment/cream colored (not dark grey)
       - Hover over an icon — subtle white tint appears (not harsh white flash)
    5. Add an item to cart or wishlist if badges are visible:
       - Badge color is TERRACOTTA (warm orange-red), not purple or magenta
    6. Confirm all nav links still work (click one)
    7. No layout shifts — header height and structure are identical to before
  </how-to-verify>
  <resume-signal>Type "approved" to proceed, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `grep "logo-full.png" components/header.tsx` returns the correct path
- `grep "bg-forest" components/header.tsx` shows the header element
- `grep "bg-neutral-50\|text-neutral-700\|hover:text-primary-600\|bg-neutral-100\|bg-primary-500\|bg-secondary-500" components/header.tsx` returns NO matches (all replaced)
- `grep "text-gray-700\|hover:bg-gray-100" components/currency-selector.tsx` returns NO matches (all replaced)
- Dev server renders without TypeScript/compilation errors
</verification>

<success_criteria>
- Wildenflower logo-full.png is visible in the header (not broken)
- Header background is forest green — no neutral-50 grey
- Nav links are parchment text with gold hover — no purple/neutral remnants
- Icon buttons are parchment with semi-transparent hover — no harsh neutral-100 flash
- Count badges are terracotta — no psychedelic primary-500/secondary-500
- CurrencySelector is legible on forest background
- All navigation, cart, wishlist, auth integrations continue to work
- User approves visual result at the checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/02-header/02-01-SUMMARY.md` following the summary template.
</output>
