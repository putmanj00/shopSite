---
phase: 11-navigation-routing
plan: 03
type: execute
wave: 2
depends_on:
  - "11-01"
  - "11-02"
files_modified: []
autonomous: false
requirements:
  - NAV-01
  - NAV-02
  - NAV-03

must_haves:
  truths:
    - "Developer has confirmed /collections redirects correctly in dev environment"
    - "Developer has confirmed the 'Wander the Shop' CTA navigates to /collections/all"
    - "Developer has reviewed grep output showing zero stale href=/collections links"
  artifacts:
    - path: "proxy.ts"
      provides: "Confirmed operational 301 redirect"
    - path: ".planning/phases/11-navigation-routing/11-03-SUMMARY.md"
      provides: "Human approval record for Phase 11"
  key_links: []
---

<objective>
Human verification checkpoint: Confirm all three NAV requirements are met by running the dev server and inspecting redirect behavior and link correctness.

Purpose: The redirect behavior (`proxy.ts`) requires a live dev server to observe — code review alone cannot confirm the 301 fires correctly. This checkpoint gives the developer a chance to click through the actual app before Phase 12 begins.

Output: Human approval that Phase 11 is complete.
</objective>

<execution_context>
@/Users/jamesputman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesputman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-verification grep confirmation</name>
  <files></files>
  <action>
Before presenting the checkpoint, run the final verification grep and present results to confirm zero stale links remain:

```bash
grep -rn 'href="/collections"' \
  /Users/jamesputman/SRC/shopSite \
  --include="*.tsx" --include="*.ts" --include="*.js" --include="*.jsx" \
  --exclude-dir=node_modules --exclude-dir=.next \
  | grep -v 'collections/all' | grep -v 'collections/\['
```

Also confirm proxy.ts contains all required patterns:
```bash
grep -n "export function proxy\|status: 301\|nextUrl.search\|matcher" /Users/jamesputman/SRC/shopSite/proxy.ts
```

Report findings. If grep returns any results, stop and fix those instances before proceeding to the human checkpoint.
  </action>
  <verify>
    <automated>
      result=$(grep -rn 'href="/collections"' /Users/jamesputman/SRC/shopSite --include="*.tsx" --include="*.ts" --include="*.js" --include="*.jsx" --exclude-dir=node_modules --exclude-dir=.next 2>/dev/null | grep -v 'collections/all' | grep -v 'collections/\['); if [ -z "$result" ]; then echo "PASS: zero stale links"; else echo "FAIL:"; echo "$result"; exit 1; fi
    </automated>
  </verify>
  <done>
    Grep confirms zero stale `href="/collections"` instances. `proxy.ts` contains all required patterns. Ready for human verification.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — redirect, CTA, and zero stale links</name>
  <files></files>
  <action>
Human verification step. Claude presents the verification instructions below and waits for approval.
  </action>
  <verify>
    <automated>MISSING — this is a human checkpoint; verification is performed by the developer in the browser</automated>
  </verify>
  <done>Developer types "approved" confirming all three NAV criteria are met.</done>
  <what-built>
    Phase 11 Navigation Routing:
    - proxy.ts: 301 redirect from /collections (and /collections/) to /collections/all with query string preservation
    - app/page.tsx: "Wander the Shop" hero CTA updated to /collections/all (NAV-02)
    - 6 additional source files: all href="/collections" instances updated to /collections/all
    - app/collections/page.tsx: deleted (dead code)
    - app/sitemap.ts: /collections bare entry removed
  </what-built>
  <how-to-verify>
    Start dev server: `cd /Users/jamesputman/SRC/shopSite && npm run dev`

    **NAV-01 — 301 Redirect:**
    Visit http://localhost:3000/collections in browser. You should land on http://localhost:3000/collections/all. Check browser address bar — it should show /collections/all, not /collections.

    **NAV-01 — Query string preservation (optional):**
    Visit http://localhost:3000/collections?sort=price-asc. Confirm address bar shows http://localhost:3000/collections/all?sort=price-asc after redirect.

    **NAV-02 — "Wander the Shop" CTA:**
    Visit http://localhost:3000 (homepage). Find the hero section. Click "Wander the Shop" button. Confirm it navigates to /collections/all.

    **NAV-03 — No stale links:**
    Review the pre-verification grep output from Task 1. Confirm zero results.
  </how-to-verify>
  <resume-signal>Type "approved" when all three criteria are confirmed, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Phase 11 is complete when the human has confirmed:
1. /collections redirects to /collections/all (301)
2. "Wander the Shop" CTA navigates to /collections/all
3. Grep shows zero stale href="/collections" links
</verification>

<success_criteria>
- Human approval recorded
- All three NAV requirements (NAV-01, NAV-02, NAV-03) visually confirmed
- Phase 12 (Navigation Labels) ready to begin
</success_criteria>

<output>
After completion, create `.planning/phases/11-navigation-routing/11-03-SUMMARY.md`
</output>
