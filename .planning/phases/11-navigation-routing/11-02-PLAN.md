---
phase: 11-navigation-routing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/page.tsx
  - components/hero.tsx
  - components/homepage/brand-story.tsx
  - components/collection-breadcrumbs.tsx
  - components/account/wishlist-preview.tsx
  - components/account/order-history.tsx
  - app/local/page.tsx
  - app/sitemap.ts
  - app/collections/page.tsx
autonomous: true
requirements:
  - NAV-02
  - NAV-03

must_haves:
  truths:
    - "The 'Wander the Shop' hero CTA navigates to /collections/all"
    - "A grep for href=\"/collections\" across all source files returns zero results"
    - "The /collections bare route has no page.tsx file (dead code removed)"
    - "The sitemap no longer lists /collections as a canonical URL"
  artifacts:
    - path: "app/page.tsx"
      provides: "Homepage with corrected hero CTA href"
      contains: "href: '/collections/all'"
    - path: "app/sitemap.ts"
      provides: "Sitemap without /collections redirect entry"
  key_links:
    - from: "app/page.tsx"
      to: "/collections/all"
      via: "EnhancedHero ctas array href prop"
      pattern: "Wander the Shop.*collections/all"
---

<objective>
Fix all 8 stale `href="/collections"` link instances across 7 source files, remove the dead `app/collections/page.tsx` route, and remove `/collections` from the sitemap.

Purpose: The proxy.ts redirect is a safety net — the codebase itself must not generate `/collections` bare links. Shoppers following any internal link go directly to `/collections/all`, never through a redirect. The NAV-02 "Wander the Shop" CTA is the primary named fix; NAV-03 covers the complete audit.

Output: 7 source files updated, 1 file deleted, sitemap cleaned. Grep for `href="/collections"` returns zero results.
</objective>

<execution_context>
@/Users/jamesputman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesputman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- All 8 href="/collections" instances confirmed via grep before planning. -->
<!-- Exact locations from codebase audit (research phase): -->

File: app/page.tsx — line 68
  Before: { label: 'Wander the Shop', href: '/collections', variant: 'primary' }
  After:  { label: 'Wander the Shop', href: '/collections/all', variant: 'primary' }
  Requirement: NAV-02

File: components/hero.tsx — lines 20, 26
  Before (line 20): href="/collections"  ("Shop Now" button)
  Before (line 26): href="/collections"  ("Browse Collections" button)
  After both:       href="/collections/all"

File: components/homepage/brand-story.tsx — line 62
  Before: href="/collections"  ("Explore the Collection" link)
  After:  href="/collections/all"

File: components/collection-breadcrumbs.tsx — line 63
  Before: href="/collections"  ("Collections" breadcrumb middle step)
  After:  href="/collections/all"

File: components/account/wishlist-preview.tsx — line 46
  Before: href="/collections"  ("Browse Products" empty-state button)
  After:  href="/collections/all"

File: components/account/order-history.tsx — line 269
  Before: href="/collections"  ("Start Shopping" empty-state button)
  After:  href="/collections/all"

File: app/local/page.tsx — line 95
  Before: href="/collections"  ("Shop Online" button)
  After:  href="/collections/all"

File: app/sitemap.ts — line 12
  Before: '/collections',   (static route entry in routes array)
  After:  (line removed — /collections is a redirect, not a canonical URL)

Delete: app/collections/page.tsx
  Reason: Dead code — proxy.ts intercepts /collections before the file router fires.
  The [handle] subdirectory (app/collections/[handle]/) is NOT deleted — keep collection detail pages.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all href="/collections" links across source files</name>
  <files>
    app/page.tsx
    components/hero.tsx
    components/homepage/brand-story.tsx
    components/collection-breadcrumbs.tsx
    components/account/wishlist-preview.tsx
    components/account/order-history.tsx
    app/local/page.tsx
  </files>
  <action>
In each file listed below, change `href="/collections"` to `href="/collections/all"`. Read each file first, make the targeted change, write back. Do not change any other content.

**app/page.tsx (line 68):**
- Find: `{ label: 'Wander the Shop', href: '/collections', variant: 'primary' }`
- Replace: `{ label: 'Wander the Shop', href: '/collections/all', variant: 'primary' }`
- This is NAV-02 — the named "Wander the Shop" hero CTA.

**components/hero.tsx (lines 20 and 26):**
- Two separate `href="/collections"` occurrences in this file ("Shop Now" and "Browse Collections").
- Change both to `href="/collections/all"`.

**components/homepage/brand-story.tsx (line 62):**
- Find: `href="/collections"` ("Explore the Collection" link)
- Replace: `href="/collections/all"`

**components/collection-breadcrumbs.tsx (line 63):**
- Find: `href="/collections"` ("Collections" breadcrumb middle step)
- Replace: `href="/collections/all"`
- Note: This breadcrumb links the middle "Collections" step — pointing to /collections/all is correct (the canonical shop page).

**components/account/wishlist-preview.tsx (line 46):**
- Find: `href="/collections"` ("Browse Products" empty-state button)
- Replace: `href="/collections/all"`

**components/account/order-history.tsx (line 269):**
- Find: `href="/collections"` ("Start Shopping" empty-state button)
- Replace: `href="/collections/all"`

**app/local/page.tsx (line 95):**
- Find: `href="/collections"` ("Shop Online" button)
- Replace: `href="/collections/all"`
  </action>
  <verify>
    <automated>
      grep -rn 'href="/collections"' /Users/jamesputman/SRC/shopSite --include="*.tsx" --include="*.ts" --include="*.js" --exclude-dir=node_modules --exclude-dir=.next 2>/dev/null | grep -v 'href="/collections/all"' | grep -v 'href="/collections/\[' || echo "PASS: zero stale href=/collections instances found"
    </automated>
  </verify>
  <done>
    All 8 `href="/collections"` instances changed to `href="/collections/all"`. The specific NAV-02 "Wander the Shop" CTA in `app/page.tsx` line 68 is updated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean up dead route and sitemap</name>
  <files>
    app/sitemap.ts
    app/collections/page.tsx
  </files>
  <action>
**Delete `app/collections/page.tsx`:**
- This file renders a collections grid that is now dead code — `proxy.ts` intercepts `/collections` before the file router fires.
- Delete the file. The `app/collections/[handle]/` subdirectory and its contents must NOT be deleted.
- Verify the [handle] subdirectory still exists after deletion.

**Edit `app/sitemap.ts` (line 12):**
- Read the file. Find the static routes array.
- Remove the `'/collections'` entry (the bare, handleless one).
- Keep `'/collections/all'` — that entry stays.
- A redirect URL should not appear in the sitemap as a canonical entry.

After the sitemap edit, the routes array should NOT contain `'/collections'` as a standalone entry. Any `/collections/all` and `/collections/${handle}` entries are correct and must remain.
  </action>
  <verify>
    <automated>
      node -e "
const fs = require('fs');
// 1. Check page.tsx is deleted
const pageExists = fs.existsSync('/Users/jamesputman/SRC/shopSite/app/collections/page.tsx');
if (pageExists) { console.error('FAIL: app/collections/page.tsx still exists'); process.exit(1); }

// 2. Check [handle] directory still exists
const handleExists = fs.existsSync('/Users/jamesputman/SRC/shopSite/app/collections/[handle]');
if (!handleExists) { console.error('FAIL: app/collections/[handle] was accidentally deleted'); process.exit(1); }

// 3. Check sitemap no longer has bare /collections entry
const sitemap = fs.readFileSync('/Users/jamesputman/SRC/shopSite/app/sitemap.ts', 'utf8');
const lines = sitemap.split('\n').filter(l => l.trim() === \"'/collections',\");
if (lines.length > 0) { console.error('FAIL: sitemap still contains bare /collections entry'); process.exit(1); }

console.log('PASS: page.tsx deleted, [handle] preserved, sitemap cleaned');
"
    </automated>
  </verify>
  <done>
    `app/collections/page.tsx` deleted. `app/collections/[handle]/` subdirectory intact. `app/sitemap.ts` no longer contains a bare `'/collections'` static route entry.
  </done>
</task>

<task type="auto">
  <name>Task 3: Final grep verification — zero stale links</name>
  <files></files>
  <action>
Run the exact success-criteria verification grep. This grep must return zero results (no output lines containing file paths).

Command:
```bash
grep -rn 'href="/collections"' \
  /Users/jamesputman/SRC/shopSite \
  --include="*.tsx" \
  --include="*.ts" \
  --include="*.js" \
  --include="*.jsx" \
  --exclude-dir=node_modules \
  --exclude-dir=.next
```

Also run a broader check for any dynamic `router.push('/collections')` patterns:
```bash
grep -rn "router\.push.*'/collections'" \
  /Users/jamesputman/SRC/shopSite \
  --include="*.tsx" \
  --include="*.ts" \
  --include="*.js" \
  --exclude-dir=node_modules \
  --exclude-dir=.next
```

If either grep returns results, identify the file+line and apply the same fix (change to `/collections/all`). Document any findings in the summary.

The phase NAV-03 success criterion is: "A codebase search for href=\"/collections\" (exact, no handle suffix) returns zero results." This task produces the evidence that criterion is met.
  </action>
  <verify>
    <automated>
      result=$(grep -rn 'href="/collections"' /Users/jamesputman/SRC/shopSite --include="*.tsx" --include="*.ts" --include="*.js" --include="*.jsx" --exclude-dir=node_modules --exclude-dir=.next 2>/dev/null | grep -v 'collections/all' | grep -v 'collections/\['); if [ -z "$result" ]; then echo "PASS: zero stale href=/collections found"; else echo "FAIL: stale links remain:"; echo "$result"; exit 1; fi
    </automated>
  </verify>
  <done>
    Grep returns zero results. NAV-03 success criterion confirmed: no `href="/collections"` (bare) exists in any .tsx/.ts/.js/.jsx source file.
  </done>
</task>

</tasks>

<verification>
After all tasks complete, run the final audit:

```bash
# Primary success criteria check (must return zero lines)
grep -rn 'href="/collections"' \
  /Users/jamesputman/SRC/shopSite \
  --include="*.tsx" --include="*.ts" --include="*.js" \
  --exclude-dir=node_modules --exclude-dir=.next \
  | grep -v 'collections/all' | grep -v 'collections/\['

# Confirm dead route is gone
ls /Users/jamesputman/SRC/shopSite/app/collections/page.tsx 2>&1

# Confirm [handle] routes are intact
ls /Users/jamesputman/SRC/shopSite/app/collections/[handle]/

# Confirm sitemap is clean
grep "'/collections'" /Users/jamesputman/SRC/shopSite/app/sitemap.ts
```
</verification>

<success_criteria>
- All 8 `href="/collections"` instances replaced with `href="/collections/all"` across 7 source files
- "Wander the Shop" CTA in `app/page.tsx` links to `/collections/all` (NAV-02)
- `app/collections/page.tsx` deleted; `app/collections/[handle]/` untouched
- `app/sitemap.ts` has no bare `/collections` entry
- Final grep returns zero results (NAV-03)
</success_criteria>

<output>
After completion, create `.planning/phases/11-navigation-routing/11-02-SUMMARY.md`
</output>
