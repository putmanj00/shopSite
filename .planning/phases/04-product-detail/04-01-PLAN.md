---
phase: 04-product-detail
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/products/[handle]/page.tsx
  - components/breadcrumbs.tsx
autonomous: true
requirements:
  - PROD-01
  - PROD-02

must_haves:
  truths:
    - "BotanicalHeader small variant is visible at the top of the product detail page, above breadcrumbs"
    - "Product detail page background renders as parchment — no bg-gray-50 visible in the main content wrapper"
    - "Breadcrumb bar background is parchment with a gold/30 bottom border — no white bar between header and content"
    - "Breadcrumb link text uses sage/inkBrown palette — no gray-* text classes remain in breadcrumbs.tsx"
    - "You May Also Like heading renders in inkBrown — no generic text color or gray-* class on that h2"
  artifacts:
    - path: "app/products/[handle]/page.tsx"
      provides: "BotanicalHeader insertion + parchment background + related products heading restyling"
      contains: "BotanicalHeader variant=\"small\""
    - path: "components/breadcrumbs.tsx"
      provides: "Sage/inkBrown breadcrumb link colors"
      contains: "text-sage"
  key_links:
    - from: "app/products/[handle]/page.tsx"
      to: "components/ui/botanical-header.tsx"
      via: "import + JSX element"
      pattern: "BotanicalHeader.*variant.*small"
    - from: "app/products/[handle]/page.tsx"
      to: "bg-parchment"
      via: "Tailwind class on min-h-screen wrapper div"
      pattern: "bg-parchment"
---

<objective>
Insert the BotanicalHeader (small variant) at the top of the product detail page shell, convert the main background and breadcrumb wrapper from gray to parchment, and restyle the "You May Also Like" related products heading to Playfair + inkBrown.

Purpose: PROD-02 places the botanical brand stamp above breadcrumbs. PROD-01 (partial) establishes the parchment canvas on the page shell — the foundation all component restyling in Plan 2 sits on.
Output: page.tsx with BotanicalHeader + parchment bg + inkBrown heading; breadcrumbs.tsx with sage/inkBrown link text.
</objective>

<execution_context>
@/Users/jamesputman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesputman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-product-detail/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert BotanicalHeader + convert page shell to parchment canvas</name>
  <files>app/products/[handle]/page.tsx</files>
  <action>
Read the full file first to understand current structure, then make targeted changes:

1. Add import at the top of the file:
   ```tsx
   import { BotanicalHeader } from '@/components/ui/botanical-header';
   ```

2. In the return JSX, insert `<BotanicalHeader variant="small" />` immediately BEFORE the main content wrapper div (which currently has `min-h-screen bg-gray-50` or similar). It must be OUTSIDE and ABOVE the parchment wrapper — same position BotanicalDividers were placed on the homepage.

3. Change the main content wrapper background from `bg-gray-50` (or whatever gray class is present) to `bg-parchment`.

4. Find the breadcrumb wrapper div (the one with `bg-white` or similar containing `<Breadcrumbs />`) and change it to `bg-parchment border-b border-gold/30`. Do NOT add white — the breadcrumb bar should flow naturally from the BotanicalHeader without a white interruption.

5. Find the "You May Also Like" heading (likely an `h2` in the related products section). Change its text color class from whatever gray/default it is to `text-ink-brown`. The `font-bold` on an `h2` already triggers Playfair Display via globals.css — no `font-playfair` class needed.

6. Check if there are any other `bg-gray-50`, `bg-white` section wrappers at the page-shell level (not card level). The locked decision keeps `bg-white rounded-lg shadow-sm` WHITE CARDS — only the outermost page/section backgrounds get parchment. Do not touch card backgrounds.

Anti-patterns:
- Do NOT touch `generateStaticParams`, `generateMetadata`, Shopify query logic, or cart/product data fetching
- Do NOT change any component imports other than adding BotanicalHeader
- Do NOT remove or alter structured data script tags or analytics trackers
- Keep `bg-white rounded-lg shadow-sm` on accordion and review card wrappers — white cards on parchment is the locked "paper on linen" look
  </action>
  <verify>
    <automated>cd /Users/jamesputman/SRC/shopSite && grep -n "BotanicalHeader" app/products/\[handle\]/page.tsx && grep -n "bg-parchment" app/products/\[handle\]/page.tsx && grep -n "border-gold" app/products/\[handle\]/page.tsx && ! grep -n "bg-gray-50" app/products/\[handle\]/page.tsx</automated>
    <manual>BotanicalHeader import and JSX element present; bg-parchment on main wrapper and breadcrumb bar; no bg-gray-50 remaining in page.tsx</manual>
  </verify>
  <done>page.tsx imports BotanicalHeader, renders `&lt;BotanicalHeader variant="small" /&gt;` above the main content wrapper, main wrapper has bg-parchment, breadcrumb bar has bg-parchment with border-gold/30, You May Also Like heading has text-ink-brown, no bg-gray-50 remains in page.tsx</done>
</task>

<task type="auto">
  <name>Task 2: Restyle breadcrumb link colors to sage/inkBrown palette</name>
  <files>components/breadcrumbs.tsx</files>
  <action>
Read the full file first. The breadcrumbs component renders a trail of links — Home → Collection → Product title.

Make these targeted class changes (visual only, no logic changes):

1. Breadcrumb link text (intermediate links like "Home", collection name): change from `text-gray-*` or `text-neutral-*` to `text-sage hover:text-ink-brown transition-colors`
2. Separator characters (/ or › between links): change from `text-gray-*` to `text-sage`
3. Current/last item (product title, non-link): change from `text-gray-900` or similar to `text-ink-brown font-medium`
4. Any `aria-current` styled element: same treatment as the current item — `text-ink-brown`

Do NOT change:
- The nav element structure, aria attributes, or hrefs
- Any Shopify data references or props
- The `line-clamp-1` utility if present (it's functional, not color-related)
- Any conditional rendering logic
  </action>
  <verify>
    <automated>cd /Users/jamesputman/SRC/shopSite && grep -n "text-sage\|text-ink-brown" components/breadcrumbs.tsx && ! grep -n "text-gray-\|text-neutral-" components/breadcrumbs.tsx</automated>
    <manual>Breadcrumb links show sage color; current item shows inkBrown; no gray-* color classes remain</manual>
  </verify>
  <done>All gray-* and neutral-* text classes in breadcrumbs.tsx replaced with sage (links/separators) and inkBrown (current item); no logic or structure changes made</done>
</task>

</tasks>

<verification>
Run TypeScript check to confirm no import errors:
```bash
cd /Users/jamesputman/SRC/shopSite && npx tsc --noEmit 2>&1 | grep -E "products|breadcrumb" | head -20
```
Expected: no errors from the two modified files.

Also confirm BotanicalHeader asset resolves:
```bash
ls /Users/jamesputman/SRC/shopSite/public/assets/images/headers/botanical-header-small.png
```
Expected: file exists (confirmed in research).
</verification>

<success_criteria>
- `app/products/[handle]/page.tsx` contains `BotanicalHeader` import and `<BotanicalHeader variant="small" />` in JSX above the main content wrapper
- Main content wrapper has `bg-parchment` (not `bg-gray-50`)
- Breadcrumb wrapper has `bg-parchment border-b border-gold/30` (not `bg-white`)
- "You May Also Like" h2 has `text-ink-brown`
- `components/breadcrumbs.tsx` has zero `text-gray-*` or `text-neutral-*` classes; link colors are sage/inkBrown
- TypeScript compiles with no errors in modified files
</success_criteria>

<output>
After completion, create `.planning/phases/04-product-detail/04-01-SUMMARY.md` following the summary template.
</output>
