---
phase: 04-product-detail
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/product-info.tsx
  - components/add-to-cart-button.tsx
  - components/variant-selector.tsx
  - components/product-accordion.tsx
  - components/sticky-add-to-cart.tsx
  - components/reviews/review-list.tsx
  - components/reviews/review-form.tsx
  - components/image-gallery.tsx
  - components/size-guide-modal.tsx
autonomous: true
requirements:
  - PROD-01

must_haves:
  truths:
    - "Product title renders in inkBrown — no text-gray-900 on the h1"
    - "Product price renders in terracotta — no default or gray color on the price span"
    - "Add to Cart button is terracotta fill with white text — no blue or primary-* color visible"
    - "Selected variant chip shows forest border + forest text on a parchment/white bg — no blue fill or blue border"
    - "Accordion section dividers and text use gold/inkBrown/earth palette — no gray-* borders or text-gray-900 headings"
    - "Sticky mobile Add to Cart button is terracotta — no bg-primary-600 (Cosmic Purple) visible at mobile viewport"
    - "Review section has no gray-* text classes remaining — all text is inkBrown/earth/sage"
  artifacts:
    - path: "components/product-info.tsx"
      provides: "Product title inkBrown, price terracotta, vendor sage, border gold/30, tags parchment bg"
      contains: "text-ink-brown"
    - path: "components/add-to-cart-button.tsx"
      provides: "Terracotta CTA button"
      contains: "bg-terracotta"
    - path: "components/variant-selector.tsx"
      provides: "Forest selected state, gold unselected border"
      contains: "border-forest"
    - path: "components/product-accordion.tsx"
      provides: "Gold dividers, inkBrown heading, sage chevron, earth body text"
      contains: "divide-gold"
    - path: "components/sticky-add-to-cart.tsx"
      provides: "Terracotta mobile CTA button replacing bg-primary-600"
      contains: "bg-terracotta"
    - path: "components/reviews/review-list.tsx"
      provides: "inkBrown/earth/sage review text; bg-white stats panel (not bg-gray-50)"
      contains: "text-ink-brown"
  key_links:
    - from: "components/add-to-cart-button.tsx"
      to: "bg-terracotta"
      via: "Tailwind class on button element"
      pattern: "bg-terracotta"
    - from: "components/sticky-add-to-cart.tsx"
      to: "bg-terracotta"
      via: "Tailwind class replacing bg-primary-600"
      pattern: "bg-terracotta"
    - from: "components/variant-selector.tsx"
      to: "border-forest"
      via: "Tailwind class on selected chip"
      pattern: "border-forest"
---

<objective>
Sweep all product detail components for gray/blue/primary color classes and replace with the Wildenflower botanical palette. Covers seven primary files plus three investigation targets (image-gallery, review-form, size-guide-modal).

Purpose: Completes PROD-01 — product detail page typography and colors fully inherit Wildenflower design tokens. After this plan and Plan 01 run, the product page is visually consistent with the parchment homepage.
Output: Seven restyled components with inkBrown/terracotta/sage/gold/forest color classes; zero blue/gray/primary-* color classes on visible elements.
</objective>

<execution_context>
@/Users/jamesputman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesputman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-product-detail/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle product-info, add-to-cart-button, and variant-selector</name>
  <files>
    components/product-info.tsx
    components/add-to-cart-button.tsx
    components/variant-selector.tsx
  </files>
  <action>
Read all three files in full before editing. Apply class substitutions below — visual only, no logic changes.

**components/product-info.tsx:**
Key class substitution table (use the research file for complete context):
- Product title h1: change text color from `text-gray-900` → `text-ink-brown` (font-bold already triggers Playfair via globals.css — no `font-playfair` needed)
- Vendor line: `text-gray-*` → `text-sage`
- Price border dividers: `border-gray-200` or `border-neutral-200` → `border-gold/30`
- Price value span: text color → `text-terracotta`
- Category/tags label: `text-gray-*` → `text-ink-brown` (for "Category:" label)
- Category/tags value: `text-gray-*` → `text-earth`
- Tag pills: `bg-gray-100 text-gray-700` → `bg-parchment text-ink-brown border border-gold/30`
- WishlistButton override: find `hover:!bg-gray-50` → `hover:!bg-parchment` (prevents gray flash on hover against parchment)
- Any remaining `text-gray-*` or `border-gray-*` visible in the product info area → replace with closest palette equivalent

Do NOT touch: `useCartStore`, variant selection logic, `handleOptionChange`, Shopify data props, `isOptionAvailable` references.

**components/add-to-cart-button.tsx:**
The button has multiple states — read the file to find them all. Replace:
- Available state: `bg-blue-600` or `bg-primary-*` → `bg-terracotta text-white hover:bg-terracotta/90 active:scale-95`
- Loading state: `bg-blue-400` or `bg-blue-*` loading variant → `bg-terracotta/60 text-white cursor-wait`
- Unavailable/sold-out state: keep `bg-gray-300 text-gray-500 cursor-not-allowed` — gray is semantically correct for disabled

Do NOT touch: button click handler, loading state logic, cart store calls.

**components/variant-selector.tsx:**
The selected/unselected/unavailable chip states. Replace:
- Selected: `bg-blue-600 text-white` or `border-blue-600` → `border-2 border-forest bg-forest/10 text-forest`
- Unselected available: `border-gray-200 text-gray-700` → `border border-gold/40 bg-white text-ink-brown hover:border-forest hover:text-forest`
- Unavailable: `text-gray-400 line-through` or similar → `border border-gold/20 bg-parchment text-sage cursor-not-allowed line-through`
- Any label text above the chip group (like "Size:" or "Color:"): `text-gray-*` → `text-ink-brown`

Do NOT touch: `handleOptionChange`, `isOptionAvailable`, `isSelected` logic, option rendering loop.
  </action>
  <verify>
    <automated>cd /Users/jamesputman/SRC/shopSite && grep -n "bg-terracotta" components/add-to-cart-button.tsx && grep -n "border-forest" components/variant-selector.tsx && grep -n "text-ink-brown\|text-terracotta" components/product-info.tsx && ! grep -n "bg-blue-\|bg-primary-\|text-gray-900\|text-gray-700" components/product-info.tsx components/add-to-cart-button.tsx components/variant-selector.tsx 2>/dev/null</automated>
    <manual>Three files show botanical colors: bg-terracotta in add-to-cart-button, border-forest in variant-selector, text-ink-brown and text-terracotta in product-info; no blue/primary/gray-900/gray-700 classes remain</manual>
  </verify>
  <done>product-info.tsx: title inkBrown, price terracotta, vendor sage, borders gold/30, tags parchment bg; add-to-cart-button.tsx: available state bg-terracotta, loading bg-terracotta/60, disabled stays gray; variant-selector.tsx: selected border-forest bg-forest/10, unselected border-gold/40, unavailable bg-parchment text-sage</done>
</task>

<task type="auto">
  <name>Task 2: Restyle accordion, sticky cart, and review components; investigate gallery/form/modal</name>
  <files>
    components/product-accordion.tsx
    components/sticky-add-to-cart.tsx
    components/reviews/review-list.tsx
    components/reviews/review-form.tsx
    components/image-gallery.tsx
    components/size-guide-modal.tsx
  </files>
  <action>
Read all six files in full before editing. Apply changes in order:

**components/product-accordion.tsx:**
- Outer container divider classes: `divide-y divide-gray-200` or `divide-neutral-*` → `divide-y divide-gold/20`
- Border classes at top/bottom of accordion: `border-t border-gray-200` → `border-t border-gold/20`
- Button/trigger text (section heading): `text-gray-900` or `font-semibold text-gray-*` → `text-ink-brown font-semibold`
- Chevron/arrow icon: `text-gray-*` → `text-sage`
- Expanded content text: `text-gray-700` or `prose text-gray-*` → `text-earth` (or `prose prose-sm max-w-none text-earth` if prose is used)

Do NOT touch: toggle logic, `isOpen` state, any click handlers.

**components/sticky-add-to-cart.tsx — CRITICAL (mobile-only, easy to miss):**
This component is `lg:hidden` so it won't show during desktop review. Include it explicitly.
- Container border: `border-t border-gray-200` → `border-t border-gold/30`
- Container bg: if `bg-white` keep it; if `bg-gray-*` → `bg-white` (white sticky bar on mobile is fine)
- Product title text: `text-neutral-900` or `text-gray-900` → `text-ink-brown`
- Price text: `text-gray-*` or default → `text-ink-brown` (or `text-terracotta` if it's the price span)
- CTA button available state: `bg-primary-600 hover:bg-primary-700 active:bg-primary-800` → `bg-terracotta hover:bg-terracotta/90 active:bg-terracotta/80`
- CTA button success state: likely `bg-green-*` → change to `bg-sage` (botanical success state) OR keep green if it's semantic (use discretion)
- CTA button unavailable: keep `bg-neutral-400 cursor-not-allowed` or gray equivalent — semantic

Do NOT touch: cart store logic, `addToCart`, `selectedVariant`, `showSuccess` state logic.

**components/reviews/review-list.tsx — CAREFUL (long component, ~240 lines):**
Systematic pass — grep for `gray-` before starting, note every instance, then replace all:
- All `text-gray-900` → `text-ink-brown`
- All `text-gray-700` / `text-gray-600` → `text-earth`
- All `text-gray-500` / `text-gray-400` → `text-sage`
- `bg-gray-50` on the rating stats panel → `bg-white` (keeps "white card on parchment" look per locked decision)
- `bg-gray-100` on filter pills or sort elements → `bg-parchment`
- `border-gray-200` → `border-gold/30`
- Star rating color: if `text-yellow-*` leave it (semantic star yellow is fine); if `text-gray-*` → `text-sage`
- Filter/sort button active state: `bg-blue-*` or `bg-primary-*` → `bg-forest text-parchment` or `bg-terracotta text-white`

After replacing, grep for `gray-` again to confirm zero survivors.

Do NOT touch: `fetchReviews`, `handleHelpful`, sort logic, filter logic, Shopify review data.

**components/reviews/review-form.tsx — INVESTIGATE:**
Read the file. If it has a submit button with `bg-blue-*` or `bg-primary-*`, replace with `bg-terracotta text-white hover:bg-terracotta/90`. Replace any `text-gray-*` input labels with `text-ink-brown` and any `border-gray-*` input borders with `border-gold/40`. If the file has no visible gray/blue classes (form inputs often inherit from globals.css), make no changes and note that in the summary.

Do NOT touch: form submission logic, validation, state management.

**components/image-gallery.tsx — INVESTIGATE:**
Read the file. Look for thumbnail border colors, navigation arrow colors, active thumbnail indicator. If gray/blue classes are present:
- Thumbnail borders: `border-gray-200` → `border-gold/30`; selected thumbnail: `border-blue-*` or `border-primary-*` → `border-forest`
- Navigation arrows: `text-gray-*` → `text-ink-brown` or `text-sage`
If no visible gray/blue classes or only structural/sizing classes, make no changes and note that in the summary.

Do NOT touch: gallery navigation logic, image loading, zoom behavior.

**components/size-guide-modal.tsx — INVESTIGATE:**
Read the file. Look for modal overlay, modal content background, heading colors, table/grid colors. If gray classes present:
- Modal content heading: `text-gray-900` → `text-ink-brown`
- Table/grid text: `text-gray-*` → `text-earth` or `text-sage` as appropriate
- Dividers: `border-gray-200` → `border-gold/30`
If the modal uses `bg-white` for content area — keep it (white modal on parchment backdrop is fine). If the modal has a backdrop overlay, it's likely `bg-black/50` — leave it.
  </action>
  <verify>
    <automated>cd /Users/jamesputman/SRC/shopSite && grep -n "bg-terracotta" components/sticky-add-to-cart.tsx && grep -n "divide-gold\|text-ink-brown" components/product-accordion.tsx && grep -n "text-ink-brown" components/reviews/review-list.tsx && ! grep -n "bg-primary-600\|bg-primary-700" components/sticky-add-to-cart.tsx && ! grep -n "text-gray-900\|text-gray-700" components/reviews/review-list.tsx 2>/dev/null</automated>
    <manual>sticky-add-to-cart has bg-terracotta; accordion has divide-gold and text-ink-brown; review-list has text-ink-brown; no bg-primary-* in sticky; no text-gray-900/700 in review-list</manual>
  </verify>
  <done>product-accordion.tsx: gold dividers, inkBrown headings, sage chevron, earth content; sticky-add-to-cart.tsx: bg-terracotta CTA, text-ink-brown product title; review-list.tsx: all gray-* → inkBrown/earth/sage, stats panel bg-white not bg-gray-50; review-form, image-gallery, size-guide-modal investigated and updated or confirmed clean</done>
</task>

</tasks>

<verification>
Run TypeScript check across all modified files:
```bash
cd /Users/jamesputman/SRC/shopSite && npx tsc --noEmit 2>&1 | grep -E "product-info|add-to-cart|variant-selector|accordion|sticky|review|gallery|size-guide" | head -30
```
Expected: no errors.

Confirm no primary-* colors survived in product components:
```bash
cd /Users/jamesputman/SRC/shopSite && grep -rn "bg-primary-\|text-primary-\|border-primary-" components/product-info.tsx components/add-to-cart-button.tsx components/variant-selector.tsx components/product-accordion.tsx components/sticky-add-to-cart.tsx components/reviews/ 2>/dev/null
```
Expected: empty output.
</verification>

<success_criteria>
- `components/add-to-cart-button.tsx`: available state is `bg-terracotta`, loading state is `bg-terracotta/60`, disabled stays gray
- `components/variant-selector.tsx`: selected state is `border-2 border-forest bg-forest/10 text-forest`
- `components/product-info.tsx`: title `text-ink-brown`, price `text-terracotta`, vendor `text-sage`, borders `border-gold/30`
- `components/product-accordion.tsx`: dividers `divide-gold/20`, headings `text-ink-brown`, content `text-earth`
- `components/sticky-add-to-cart.tsx`: CTA button `bg-terracotta` (not `bg-primary-600`)
- `components/reviews/review-list.tsx`: zero `text-gray-900` or `text-gray-700` survivors; stats panel `bg-white` (not `bg-gray-50`)
- TypeScript compiles clean across all modified files
- `bg-primary-*` grep across all modified component files returns empty
</success_criteria>

<output>
After completion, create `.planning/phases/04-product-detail/04-02-SUMMARY.md` following the summary template.
</output>
