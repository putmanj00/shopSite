---
phase: 18-security-dev-tooling
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - SEC-02
  - SEC-03

must_haves:
  truths:
    - "A gitleaks full git history scan returns zero secret findings (or findings are documented and keys rotated)."
    - "All .env* file patterns are confirmed present in .gitignore."
    - "No .env* files exist in git history (git log confirms none were ever committed)."
  artifacts:
    - path: ".gitignore"
      provides: "Excludes all .env* files from git tracking."
      contains: ".env*"
  key_links:
    - from: ".gitignore"
      to: ".env* files"
      via: "glob pattern .env* on its own line"
      pattern: "^\\.env\\*"
---

<objective>
Run a one-time gitleaks audit of the full git history to confirm no secrets were ever committed, and verify that all .env* files are properly excluded from git tracking, addressing SEC-02 and SEC-03.

Purpose: Close the barn door retroactively — confirm the git history is clean of secrets before moving forward with CI/CD pipeline and public deployment.
Output: Confirmed clean git history (zero gitleaks findings) and verified .gitignore coverage of all .env* patterns. If findings are detected, they are documented in the commit message and affected keys are rotated.
</objective>

<execution_context>
@/Users/jamesputman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesputman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- gitleaks v8 commands (install via brew install gitleaks) -->
<!-- History scan — scans full git log -p output for secrets: -->
<!--   gitleaks git -v . -->
<!-- Pre-commit staged scan — used later in lefthook.yml (Plan 18-03): -->
<!--   gitleaks protect --staged --source . -->

<!-- .gitignore current state (already contains .env*): -->
<!-- # env files (can opt-in for committing if needed) -->
<!-- .env*                                               -->
<!-- next-env.d.ts                                      -->

<!-- If gitleaks produces findings: -->
<!--   - Document the finding in the plan summary (filename, line, secret type) -->
<!--   - Rotate the affected credential immediately (new key from provider) -->
<!--   - DO NOT rewrite git history (user decision from CONTEXT.md) -->
<!--   - Update the live .env.local with the new credential -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify gitleaks is installed</name>
  <files></files>
  <action>
    Check if gitleaks is installed by running `gitleaks version`. If it returns a version string, proceed. If the command is not found, install it via `brew install gitleaks` and then verify again. gitleaks is a system binary — do NOT add it to package.json.
  </action>
  <verify>
    <automated>gitleaks version</automated>
  </verify>
  <done>gitleaks version command outputs a version string (e.g., "v8.x.x"). The binary is available on PATH.</done>
</task>

<task type="auto">
  <name>Task 2: Run full git history secrets scan</name>
  <files></files>
  <action>
    Run `gitleaks git -v /Users/jamesputman/SRC/shopSite` to scan the complete git history for secrets. This command scans all commits via `git log -p` output.

    Interpret the result:
    - Exit code 0: No findings. Record "gitleaks git scan: CLEAN — zero findings" in the plan summary.
    - Exit code 1: Findings detected. For each finding, record: the file path, commit hash, and secret type. Then rotate the affected credential immediately by generating a new key from the provider (Shopify Admin, Resend, etc.) and updating .env.local. Do NOT rewrite git history — user decision from CONTEXT.md explicitly prohibits this.

    Run the scan from the project root:
    ```bash
    cd /Users/jamesputman/SRC/shopSite && gitleaks git -v .
    ```
  </action>
  <verify>
    <automated>cd /Users/jamesputman/SRC/shopSite && gitleaks git -v . ; echo "gitleaks exit code: $?"</automated>
  </verify>
  <done>gitleaks scan completes. Either exits 0 (clean) or findings are documented and affected credentials rotated. Either way the result is captured for the summary.</done>
</task>

<task type="auto">
  <name>Task 3: Verify .env* files are in .gitignore and absent from git history</name>
  <files></files>
  <action>
    Perform two verifications:

    1. Confirm `.env*` pattern is in .gitignore:
    ```bash
    grep -n '\.env\*' /Users/jamesputman/SRC/shopSite/.gitignore
    ```
    Expected: at least one line containing `.env*`

    2. Confirm no .env* files appear in git history:
    ```bash
    cd /Users/jamesputman/SRC/shopSite && git log --all --full-history -- '.env*' --oneline
    ```
    Expected: no output (empty) means no .env* file was ever committed.

    If any .env* file appears in git history, document the finding in the summary (filename and commit hash). Do NOT rewrite history.

    No files need to be created or modified — .gitignore already contains `.env*` (confirmed before planning).
  </action>
  <verify>
    <automated>grep -c '\.env\*' /Users/jamesputman/SRC/shopSite/.gitignore && cd /Users/jamesputman/SRC/shopSite && git log --all --full-history -- '.env*' --oneline | wc -l</automated>
  </verify>
  <done>grep returns count >= 1 (pattern present in .gitignore). git log command returns 0 lines (no .env* files in history). SEC-03 confirmed satisfied.</done>
</task>

</tasks>

<verification>
After all tasks:
1. `gitleaks version` outputs a version string — binary is available
2. `gitleaks git -v .` completes — result is zero findings OR findings are documented and credentials rotated
3. `grep '\.env\*' .gitignore` returns at least one match
4. `git log --all --full-history -- '.env*' --oneline` returns no output (empty)
</verification>

<success_criteria>
Git history confirmed clean via gitleaks scan (zero findings or documented findings with rotated credentials). .env* files confirmed in .gitignore and absent from all commits. SEC-02 and SEC-03 are satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/18-security-dev-tooling/18-02-SUMMARY.md`

The summary MUST include:
- gitleaks scan result: "CLEAN — zero findings" or list of findings with action taken (rotated key for X, Y)
- .gitignore verification result: confirmed present / not present
- git history verification result: confirmed clean / findings listed
</output>
