---
phase: 19-playwright-e2e-tests
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - .planning/phases/19-playwright-e2e-tests/test-product.md
autonomous: false
requirements:
  - E2E-03
  - E2E-04
  - E2E-05
  - E2E-06

must_haves:
  truths:
    - "A 'Test Product' exists in Shopify admin with a known handle and title"
    - "The Test Product is published and reachable at /products/<handle> on the dev server"
    - "Plans 03 and 04 can reference TEST_PRODUCT_HANDLE and TEST_PRODUCT_TITLE as confirmed values"
  artifacts:
    - path: ".planning/phases/19-playwright-e2e-tests/test-product.md"
      provides: "Record of Test Product handle and title for executor use in plans 03 and 04"
      contains: "handle"
  key_links:
    - from: "Shopify admin"
      to: "/products/<handle>"
      via: "Storefront API product lookup"
      pattern: "Test Product"
---

<objective>
Human checkpoint: Create a dedicated "Test Product" in the Shopify Wildenflower admin with a clearly QA-purpose name, confirm its handle, and record the details so Plans 03 and 04 can reference them.

Purpose: E2E-03 (PDP), E2E-04 (add-to-cart), E2E-05 (checkout redirect), and E2E-06 (search) all target a specific, stable Shopify product by handle and title. Using a dedicated test product ensures tests never break due to real catalog changes.
Output: A confirmed Test Product handle and title, recorded in `test-product.md` for executor reference.
</objective>

<execution_context>
@/Users/jamesputman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesputman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-playwright-e2e-tests/19-CONTEXT.md
@.planning/phases/19-playwright-e2e-tests/19-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create Test Product in Shopify admin</name>
  <action>
This is a human-only action. Claude cannot create Shopify products — there is no CLI or API available in this phase.

The user must log in to the Shopify Wildenflower admin and create a new product with these properties:

1. Title: `Wildenflower Test Product` (or similar clearly QA-purpose name — avoids accidental deletion)
2. Status: Active (Published)
3. Price: Any price (e.g., $1.00)
4. Inventory: Set to track quantity, enter at least 1 in stock (the "Add to Cart" button is disabled when out of stock)
5. Image: Add any image (the PDP test checks that an image is visible)
6. Description: Any text (e.g., "QA test product — do not delete")

After saving, note the handle from the product URL. The handle is the slug after `/products/` in the storefront URL (e.g., if the product URL is `/products/wildenflower-test-product`, the handle is `wildenflower-test-product`).

Verify the product appears on the dev server: visit `http://localhost:3000/products/<handle>` and confirm the page loads with the product title visible.
  </action>
  <verify>
    <automated>MISSING — requires human confirmation. User must reply with handle and title before Task 2 can run.</automated>
  </verify>
  <done>
User has confirmed:
- Handle: `<exact-handle>` (the Shopify URL slug, e.g., `wildenflower-test-product`)
- Title: `<exact-title>` (the product title, e.g., `Wildenflower Test Product`)
- Product is reachable at `http://localhost:3000/products/<handle>` with title and image visible
- Product has at least 1 unit in stock (Add to Cart button is enabled)

Resume signal: User replies with "Handle: X, Title: Y"
  </done>
</task>

<task type="auto">
  <name>Task 2: Record Test Product details for downstream plans</name>
  <files>.planning/phases/19-playwright-e2e-tests/test-product.md</files>
  <action>
After the user provides the handle and title in the resume signal, create `.planning/phases/19-playwright-e2e-tests/test-product.md` with this content (substituting the confirmed values):

```markdown
# Test Product — E2E Test Reference

Purpose: Stable Shopify product for E2E-03, E2E-04, E2E-05, E2E-06 test suites.

## Details

| Field | Value |
|-------|-------|
| Handle | `<confirmed-handle>` |
| Title | `<confirmed-title>` |
| Storefront URL | `/products/<confirmed-handle>` |

## Usage in Tests

```typescript
const TEST_PRODUCT_HANDLE = '<confirmed-handle>';
const TEST_PRODUCT_TITLE = '<confirmed-title>';
```

## Notes
- Do not delete this product — it is the target for automated E2E tests
- Do not rename it — the search test (E2E-06) searches by exact title
- Keep at least 1 unit in stock — the add-to-cart test requires the button to be enabled
```

The executor of Plans 03 and 04 will reference this file to get the confirmed constants.
  </action>
  <verify>
    <automated>test -f /Users/jamesputman/SRC/shopSite/.planning/phases/19-playwright-e2e-tests/test-product.md && grep -q "handle" /Users/jamesputman/SRC/shopSite/.planning/phases/19-playwright-e2e-tests/test-product.md && echo "EXISTS_WITH_HANDLE"</automated>
  </verify>
  <done>`test-product.md` exists with the confirmed handle and title. No placeholder strings (`<confirmed-handle>`) remain. The file is ready for Plans 03 and 04 executors to reference.</done>
</task>

</tasks>

<verification>
1. A Shopify product with a QA-purpose name exists and is published on the Wildenflower store
2. The product is reachable at `http://localhost:3000/products/<handle>` with title and image visible
3. `.planning/phases/19-playwright-e2e-tests/test-product.md` contains the confirmed handle and title (no placeholders)
4. Product has at least 1 unit in stock (Add to Cart button is enabled)
</verification>

<success_criteria>
- Test Product exists in Shopify admin with confirmed handle and title
- Product visible on storefront at /products/<handle>
- Handle and title recorded in test-product.md with no placeholder values
- Plans 03 and 04 can proceed with known, stable constants
</success_criteria>

<output>
After completion, create `.planning/phases/19-playwright-e2e-tests/19-02-SUMMARY.md` following the summary template.
</output>
