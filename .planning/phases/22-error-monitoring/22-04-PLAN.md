---
phase: 22-error-monitoring
plan: 04
type: execute
wave: 4
depends_on: [22-03]
files_modified:
  - app/sentry-test/page.tsx
  - app/sentry-test/route.ts
autonomous: false
requirements: [MON-01, MON-02, MON-03]

must_haves:
  truths:
    - "The Sentry dashboard shows a wildenflower project receiving events"
    - "A triggered test error appears in Sentry within 60 seconds"
    - "Running the dev server locally sends zero events to Sentry"
    - "The /sentry-test route is deleted from the codebase after verification"
    - "Both server-side and client-side errors appear in Sentry with readable TypeScript source"
  artifacts:
    - path: "app/sentry-test/page.tsx"
      provides: "DELETED after dashboard verification"
    - path: "app/sentry-test/route.ts"
      provides: "DELETED after dashboard verification"
  key_links:
    - from: "Sentry dashboard"
      to: "wildenflower project"
      via: "Events visible within 60s of triggering test page"
      pattern: "n/a — manual verification"
---

<objective>
Deploy to the production Vercel project (where SENTRY_DSN is set), trigger test errors via /sentry-test, confirm both server and client events appear in the Sentry dashboard, then delete the test routes and commit the clean state.

Purpose: This is the production verification that all three MON requirements are met. The test page must fire against the production deployment (where the DSN is configured) — local dev will not send events (MON-02 verified by absence). After verification, the test page deletion is a critical cleanup step — leaving it would generate noise and expose error traces to visitors.

Output: Sentry dashboard shows real events from wildenflower production. Test routes deleted. Clean git state. Phase 22 complete.
</objective>

<execution_context>
@/Users/jamesputman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesputman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-error-monitoring/22-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify Sentry events arrive from production deployment</name>
  <what-built>Plans 01-03 installed @sentry/nextjs, created four config files, wired error boundaries, created a /sentry-test page, and set SENTRY_DSN + NEXT_PUBLIC_SENTRY_DSN in Vercel production env. A production deployment must be triggered first.</what-built>
  <how-to-verify>
    Step 1 — Trigger a production deployment:
    Open a PR from the current branch against main, wait for CI to pass, then merge to main.
    The deploy-prod job runs after merge (requires manual approval in GitHub Actions -> production environment gate).
    Approve the deploy. Wait for deploy-prod to complete.

    Step 2 — Verify MON-02 (no dev events):
    Run locally: `npm run dev`
    Visit http://localhost:3000 and browse around.
    Check the Sentry dashboard — confirm zero events appear from localhost.
    If any events appear with environment: 'development', the DSN-gating is broken — do not proceed.

    Step 3 — Trigger server-side error (MON-03 server path):
    Visit the production URL: https://wildenflower.com/sentry-test/route (or use curl):
    `curl https://wildenflower.com/api/sentry-test` (if route is at app/sentry-test/route.ts, the URL is /sentry-test)
    Actually: visit https://wildenflower.com/sentry-test in a browser (the page load triggers both).

    Step 4 — Trigger client-side error (MON-03 client path):
    The page at https://wildenflower.com/sentry-test fires an unhandled Promise.reject() on load.
    Wait 60 seconds, then check the Sentry dashboard.

    Step 5 — Confirm in Sentry dashboard:
    Go to https://sentry.io -> wildenflower project -> Issues
    Confirm two issues appear:
      - "Sentry server test — delete this route after verifying dashboard" (server error, from GET /sentry-test)
      - "Sentry client test — delete this page after verifying dashboard" (client error, from page load)
    Click into each issue and confirm the stack trace shows readable TypeScript file paths (e.g., app/sentry-test/page.tsx), not minified chunk references.

    Step 6 — Verify MON-01 and MON-03 criteria:
    - Sentry dashboard shows the wildenflower project receiving events: YES / NO
    - Both server and client events visible within 60s: YES / NO
    - Stack traces show readable TypeScript (not chunk.js:1:23942): YES / NO

    If any check fails, describe the failure — do not type "approved".
  </how-to-verify>
  <resume-signal>Type "approved" after confirming all three checks pass (events visible, both paths captured, readable traces)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Delete temporary test routes and commit clean state</name>
  <files>app/sentry-test/page.tsx, app/sentry-test/route.ts</files>
  <action>
    Delete both temporary test files:
    ```bash
    git rm /Users/jamesputman/SRC/shopSite/app/sentry-test/page.tsx
    git rm /Users/jamesputman/SRC/shopSite/app/sentry-test/route.ts
    rmdir /Users/jamesputman/SRC/shopSite/app/sentry-test 2>/dev/null || true
    ```

    Verify deletion:
    - `app/sentry-test/` directory no longer exists
    - `git status` shows the deletions staged

    Commit:
    ```bash
    git commit -m "chore(22): remove temporary Sentry test routes after dashboard verification"
    ```

    WHY: The test page generates noise in the Sentry dashboard (repeated "Sentry client test" events from any user who visits /sentry-test) and reveals server-side error stacks to anyone who discovers the URL. It must be deleted immediately after verification.
  </action>
  <verify>
    <automated>test ! -d /Users/jamesputman/SRC/shopSite/app/sentry-test && echo "sentry-test directory deleted" && grep -r "sentry-test" /Users/jamesputman/SRC/shopSite/app/ 2>/dev/null || echo "no references remain"</automated>
  </verify>
  <done>app/sentry-test/ directory deleted and committed; no references to sentry-test remain in app/; npm run typecheck passes</done>
</task>

</tasks>

<verification>
1. Sentry dashboard wildenflower project shows at least 2 issues: the server test error and client test error
2. Stack traces in Sentry show readable TypeScript file paths (source maps uploaded successfully)
3. `app/sentry-test/` directory does not exist in the codebase
4. `npm run typecheck` passes on the cleaned codebase
5. Local dev server (NODE_ENV=development) generates zero Sentry events — SENTRY_DSN undefined in dev
</verification>

<success_criteria>
All three MON requirements confirmed:
- MON-01: @sentry/nextjs installed and configured (server + client capture active in production)
- MON-02: Dev server sends zero events (DSN-gating confirmed by absence)
- MON-03: Both server and client errors appear in Sentry dashboard with readable TypeScript traces
Test routes deleted. Phase 22 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/22-error-monitoring/22-error-monitoring-04-SUMMARY.md`
</output>
