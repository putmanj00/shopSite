---
phase: 22-error-monitoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [MON-01, MON-02]

must_haves:
  truths:
    - "A Sentry account exists with a wildenflower project"
    - "The Sentry DSN value is known and ready to use in config files"
    - "The Sentry auth token is known and ready for GitHub Secrets + Vercel env"
    - "The Sentry org slug is known and ready for withSentryConfig"
  artifacts:
    - path: ".planning/phases/22-error-monitoring/sentry-credentials.md"
      provides: "Org slug, DSN values (SENTRY_DSN + NEXT_PUBLIC_SENTRY_DSN), auth token placeholder note"
  key_links:
    - from: "sentry-credentials.md"
      to: "22-02-PLAN.md"
      via: "org slug and DSN used in config files"
      pattern: "org: 'YOUR_ORG_SLUG'"

user_setup:
  - service: sentry
    why: "Error monitoring dashboard — account must exist before DSN and auth token values are available for code configuration"
    env_vars:
      - name: SENTRY_DSN
        source: "Sentry Dashboard -> wildenflower project -> Settings -> Client Keys (DSN)"
      - name: NEXT_PUBLIC_SENTRY_DSN
        source: "Same DSN value as SENTRY_DSN — set both in Vercel production environment"
      - name: SENTRY_AUTH_TOKEN
        source: "Sentry Dashboard -> User Settings -> Auth Tokens -> Create New Token (project:releases + org:read scopes)"
    dashboard_config:
      - task: "Create Sentry organization"
        location: "https://sentry.io/signup — choose Developer (free) plan"
      - task: "Create project named 'wildenflower' with platform 'Next.js'"
        location: "Sentry Dashboard -> Projects -> Create Project"
      - task: "Set SENTRY_DSN and NEXT_PUBLIC_SENTRY_DSN in Vercel production env"
        location: "Vercel Dashboard -> shopsite-prod project -> Settings -> Environment Variables"
      - task: "Set SENTRY_AUTH_TOKEN in Vercel production env"
        location: "Vercel Dashboard -> shopsite-prod project -> Settings -> Environment Variables"
      - task: "Set SENTRY_AUTH_TOKEN as GitHub Actions secret"
        location: "GitHub -> shopSite repo -> Settings -> Secrets and variables -> Actions -> New repository secret"
---

<objective>
Create the Sentry account and project, then capture all credential values needed for code configuration. No code changes in this plan — this is the mandatory human prerequisite so Plans 02-03 have concrete DSN and auth token values to use.

Purpose: @sentry/nextjs requires a real DSN and auth token before any config files can be tested. The org slug is needed for withSentryConfig. All three values come only from the Sentry dashboard.

Output: A credentials reference file at `.planning/phases/22-error-monitoring/sentry-credentials.md` with the org slug and confirmation that both env vars (SENTRY_DSN, NEXT_PUBLIC_SENTRY_DSN, SENTRY_AUTH_TOKEN) are set in Vercel prod and GitHub Secrets.
</objective>

<execution_context>
@/Users/jamesputman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesputman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-error-monitoring/22-CONTEXT.md
@.planning/phases/22-error-monitoring/22-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create Sentry account, project, and credentials</name>
  <what-built>Nothing yet — this plan gates all subsequent code work on having real Sentry credentials.</what-built>
  <how-to-verify>
    Complete these steps in order:

    1. Go to https://sentry.io/signup and create a new account (Developer free plan — 5,000 errors/month)
    2. Create organization (pick any name — the slug in the URL is what matters, e.g., "wildenflower-shop")
    3. Create a project: Platform = "Next.js", Project name = "wildenflower"
    4. Copy the DSN from the project setup page (or: Settings -> Client Keys (DSN) -> DSN value)
       The DSN looks like: https://abc123xyz@o123456.ingest.sentry.io/789012
    5. Create an auth token: User Settings (top-right avatar) -> Auth Tokens -> Create New Token
       Required scopes: `project:releases` and `org:read`
    6. Note your Sentry org slug: visible in the Sentry URL as sentry.io/organizations/YOUR-SLUG/
    7. Set these env vars in Vercel Dashboard -> shopsite-prod project -> Settings -> Environment Variables:
       - SENTRY_DSN = (the DSN value from step 4)
       - NEXT_PUBLIC_SENTRY_DSN = (same DSN value — used for browser-side init)
       - SENTRY_AUTH_TOKEN = (the auth token from step 5) — set as "Sensitive" encrypted value
       All three should target "Production" environment only (not Preview or Development).
    8. Set SENTRY_AUTH_TOKEN as a GitHub Actions secret:
       GitHub -> shopSite repo -> Settings -> Secrets and variables -> Actions -> New repository secret
       Name: SENTRY_AUTH_TOKEN, Value: (the auth token from step 5)
    9. Create `.planning/phases/22-error-monitoring/sentry-credentials.md` with:
       ```
       # Sentry Credentials Reference (Phase 22)
       Org slug: YOUR_ORG_SLUG
       Project: wildenflower
       DSN env var names: SENTRY_DSN (server), NEXT_PUBLIC_SENTRY_DSN (client)
       Auth token: stored in GitHub Secrets + Vercel prod env as SENTRY_AUTH_TOKEN
       Vercel env set: yes
       GitHub secret set: yes
       ```
       (Do not write actual token values to this file — just the org slug and confirmation status)
  </how-to-verify>
  <resume-signal>Type "done" after completing all 9 steps and creating the credentials file</resume-signal>
</task>

</tasks>

<verification>
After checkpoint completion:
- `.planning/phases/22-error-monitoring/sentry-credentials.md` exists with org slug filled in
- Vercel shopsite-prod env shows SENTRY_DSN, NEXT_PUBLIC_SENTRY_DSN, SENTRY_AUTH_TOKEN set
- GitHub repo secrets shows SENTRY_AUTH_TOKEN
</verification>

<success_criteria>
Org slug, DSN value, and SENTRY_AUTH_TOKEN are all available — Vercel prod env and GitHub Secrets are set. Plan 02 can now use these values without further manual intervention.
</success_criteria>

<output>
After completion, create `.planning/phases/22-error-monitoring/22-error-monitoring-01-SUMMARY.md`
</output>
