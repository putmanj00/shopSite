---
phase: 03-homepage
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/page.tsx
  - components/featured-products.tsx
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - HOME-04
  - HOME-05

must_haves:
  truths:
    - "Homepage root background renders as warm parchment (#F5EDD6) — no grey visible"
    - "Hero section shows botanical-header-large.png image with 'Made by hand. Found by heart.' heading"
    - "Three BotanicalDivider components are visible between homepage sections (after hero, after categories, after featured products)"
    - "Featured products heading reads 'Freshly Gathered' with terracotta 'View All' link (no blue)"
    - "PersonalizedRecommendations section has no grey background island — parchment inherits from parent"
  artifacts:
    - path: "app/page.tsx"
      provides: "Homepage layout with parchment bg, botanical hero props, BotanicalDivider insertions"
      contains: "bg-[#F5EDD6]"
    - path: "components/featured-products.tsx"
      provides: "Freshly Gathered heading with terracotta View All link"
      contains: "Freshly Gathered"
  key_links:
    - from: "app/page.tsx"
      to: "components/ui/botanical-divider.tsx"
      via: "named import { BotanicalDivider }"
      pattern: "BotanicalDivider"
    - from: "app/page.tsx"
      to: "components/homepage/enhanced-hero.tsx"
      via: "EnhancedHero props"
      pattern: "botanical-header-large"
---

<objective>
Apply the parchment background, botanical hero image and copy, BotanicalDivider placements, and "Freshly Gathered" heading to the homepage layout.

Purpose: Converts the homepage shell from psychedelic to botanical — setting the warm parchment canvas and inserting the three structural dividers that give the page botanical rhythm.
Output: Updated app/page.tsx (background + EnhancedHero props + 3 BotanicalDividers) and updated components/featured-products.tsx (heading + link color).
</objective>

<execution_context>
@/Users/jamesputman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesputman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-homepage/03-CONTEXT.md
@.planning/phases/03-homepage/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Homepage layout — parchment background, botanical hero, BotanicalDividers</name>
  <files>app/page.tsx</files>
  <action>
Read app/page.tsx in full first. Then make the following targeted changes:

1. ROOT BACKGROUND (HOME-01)
   - Find: `<div className="min-h-screen bg-neutral-50">`
   - Replace with: `<div className="min-h-screen bg-[#F5EDD6]">`

2. PERSONALIZEDRECOMMENDATIONS WRAPPER (HOME-01)
   - Find the wrapper div around `<PersonalizedRecommendations />` that has `className="bg-zinc-50"`
   - Remove only `bg-zinc-50` from its className (or remove className entirely if that is its only class). Parchment will inherit from parent.

3. ENHANCED HERO PROPS (HOME-02)
   - Find the `<EnhancedHero` JSX call in page.tsx
   - Update these props:
     - `heading="Made by hand. Found by heart."`
     - `subheading` — write original warm Wildenflower botanical copy (Claude's discretion). Example tone: "Gathered from maker hands and meadow roots. Every piece finds the heart that needs it." Keep it to 1–2 short sentences, unhurried.
     - `backgroundImage="/assets/images/headers/botanical-header-large.png"`
     - `overlayOpacity={25}` (lower than default 50 — botanical illustration reads better with lighter overlay)
     - `ctas` — update labels to Wildenflower voice: `{ label: 'Wander the Shop', href: '/collections', variant: 'primary' }` and `{ label: 'Our Story', href: '#brand-story', variant: 'secondary' }`. Do NOT change href values beyond these examples — use whatever hrefs are currently in place and only update the label text.
   - Do NOT modify enhanced-hero.tsx component internals — change call site props only.

4. BOTANICAL DIVIDERS (HOME-04)
   - Add import at top of file: `import { BotanicalDivider } from '@/components/ui/botanical-divider';`
   - Insert `<BotanicalDivider variant="wildflower" />` immediately after the `<EnhancedHero ... />` closing tag
   - Insert `<BotanicalDivider variant="fern-mushroom" />` immediately after the `<CategoryCards ... />` (or equivalent category section) closing tag
   - Insert `<BotanicalDivider variant="fern-spiral" />` immediately after the `<FeaturedProducts ... />` closing tag
   - BotanicalDivider is a named export — use `{ BotanicalDivider }` not default import.
   - Insert dividers at page layout level (between section components), NOT inside any section component.
  </action>
  <verify>
    <automated>cd /Users/jamesputman/SRC/shopSite && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Run `npm run dev` and open http://localhost:3000. Confirm: (1) page background is warm parchment not grey, (2) hero shows botanical image with "Made by hand. Found by heart." heading, (3) three botanical dividers visible between sections.</manual>
    <sampling_rate>run after this task, before Task 2</sampling_rate>
  </verify>
  <done>
    - app/page.tsx has `bg-[#F5EDD6]` on root div
    - EnhancedHero receives `backgroundImage="/assets/images/headers/botanical-header-large.png"`, `heading="Made by hand. Found by heart."`, and `overlayOpacity={25}`
    - Three `<BotanicalDivider>` imports and insertions present (wildflower, fern-mushroom, fern-spiral)
    - PersonalizedRecommendations wrapper has no `bg-zinc-50`
    - TypeScript compiles clean
  </done>
</task>

<task type="auto">
  <name>Task 2: FeaturedProducts — "Freshly Gathered" heading and terracotta link</name>
  <files>components/featured-products.tsx</files>
  <action>
Read components/featured-products.tsx in full first. Then make two targeted changes:

1. HEADING (HOME-05)
   - Find the `<h2>` that contains "Featured Products"
   - Change text to "Freshly Gathered"
   - Update classes: `text-zinc-900` → `text-forest`, add `font-heading` class if not present
   - Result: `<h2 className="text-3xl font-bold text-forest md:text-4xl font-heading">Freshly Gathered</h2>`

2. VIEW ALL LINK COLOR (HOME-05)
   - Find the "View All" or "View All Products" link adjacent to the heading
   - Change `text-blue-600` → `text-terracotta`
   - Change `hover:text-blue-700` → `hover:text-terracotta/80`
   - Do NOT change the link href or any other attribute.

Do NOT touch any product grid JSX, Shopify data fetching, or card components.
  </action>
  <verify>
    <automated>cd /Users/jamesputman/SRC/shopSite && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>In dev server at http://localhost:3000, scroll to featured products section. Confirm heading reads "Freshly Gathered" in forest green, and "View All" link is terracotta not blue.</manual>
    <sampling_rate>run after this task completes</sampling_rate>
  </verify>
  <done>
    - Heading text is "Freshly Gathered"
    - Heading uses `text-forest font-heading` classes
    - "View All" link uses `text-terracotta hover:text-terracotta/80`
    - TypeScript compiles clean
    - No product grid or Shopify logic changed
  </done>
</task>

</tasks>

<verification>
Run `npx tsc --noEmit` from project root — must produce no errors.
Grep confirms: `grep -n "bg-\[#F5EDD6\]" app/page.tsx` finds the root div.
Grep confirms: `grep -n "botanical-header-large" app/page.tsx` finds the hero backgroundImage prop.
Grep confirms: `grep -c "BotanicalDivider" app/page.tsx` returns at least 4 (1 import + 3 usages).
Grep confirms: `grep -n "Freshly Gathered" components/featured-products.tsx` finds the heading.
Grep confirms: `grep -n "text-terracotta" components/featured-products.tsx` finds the link.
</verification>

<success_criteria>
- Homepage root background is parchment (bg-[#F5EDD6]) — no neutral-50 or zinc-50 grey visible
- EnhancedHero renders botanical-header-large.png with "Made by hand. Found by heart." heading and overlayOpacity=25
- Three BotanicalDivider components inserted at correct page-level positions (after hero, after categories, after featured products)
- PersonalizedRecommendations has no grey bg override
- Featured products heading is "Freshly Gathered" with terracotta View All link
- TypeScript compiles clean after all changes
</success_criteria>

<output>
After completion, create `.planning/phases/03-homepage/03-01-SUMMARY.md` following the summary template.
</output>
