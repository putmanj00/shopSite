---
phase: 03-homepage
plan: "04"
type: execute
wave: 3
depends_on:
  - "03-01"
files_modified:
  - components/homepage/enhanced-hero.tsx
autonomous: false
gap_closure: true
requirements:
  - HOME-02

must_haves:
  truths:
    - "Hero heading 'Made by hand. Found by heart.' renders entirely in white — no purple or violet tint on any word"
    - "Primary CTA button ('Wander the Shop') background is terracotta (#C8642A), not Cosmic Purple (#7C3AED)"
    - "No text-primary-* or bg-primary-* classes remain in enhanced-hero.tsx"
  artifacts:
    - path: "components/homepage/enhanced-hero.tsx"
      provides: "EnhancedHero component with flat heading render and terracotta CTA"
      contains: "bg-terracotta"
  key_links:
    - from: "components/homepage/enhanced-hero.tsx"
      to: "heading prop"
      via: "flat h1 render (no word-splitting map)"
      pattern: "<h1[^>]*>\\{heading\\}<\\/h1>"
---

<objective>
Gap closure — remove the Cosmic Purple word-coloring and CTA button color from EnhancedHero internals.

Purpose: The VERIFICATION.md gap report found that enhanced-hero.tsx lines 72-81 split the heading by words and wrap the last two in `text-primary-300` (#d8b4fe, Cosmic Purple light), and line 100 applies `bg-primary-600` (#7C3AED, Cosmic Purple) to the primary CTA button. With heading "Made by hand. Found by heart.", the words "by heart." render purple and the "Wander the Shop" button is purple — directly contradicting the Phase 3 goal of no psychedelic colors on the homepage.

Output: `components/homepage/enhanced-hero.tsx` with flat heading render and terracotta primary CTA button.
</objective>

<execution_context>
@/Users/jamesputman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesputman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-homepage/03-VERIFICATION.md
@components/homepage/enhanced-hero.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove word-coloring logic and fix CTA button color in enhanced-hero.tsx</name>
  <files>components/homepage/enhanced-hero.tsx</files>
  <action>
Two targeted edits to `components/homepage/enhanced-hero.tsx`. Do NOT change any other lines — layout, overlay, trust badges, scroll indicator, and default props are all untouched.

**Edit 1 — Remove word-splitting heading logic (lines 72-82):**

Replace the entire `<h1>` block that uses `.split(' ').map(...)` with a flat render:

Current (lines 71-82):
```tsx
          <h1 className="text-4xl md:text-5xl lg:text-6xl font-bold text-white leading-tight mb-6 font-heading">
            {heading.split(' ').map((word, index, arr) => (
              <span key={index}>
                {index >= arr.length - 2 ? (
                  <span className="text-primary-300">{word}</span>
                ) : (
                  word
                )}
                {index < arr.length - 1 ? ' ' : ''}
              </span>
            ))}
          </h1>
```

Replace with:
```tsx
          <h1 className="text-4xl md:text-5xl lg:text-6xl font-bold text-white leading-tight mb-6 font-heading">
            {heading}
          </h1>
```

**Edit 2 — Change primary CTA button color (line 100):**

Current:
```tsx
                      : 'bg-primary-600 text-white hover:bg-primary-500'
```

Replace with:
```tsx
                      : 'bg-terracotta text-white hover:bg-terracotta/90'
```

Rationale: `bg-terracotta` resolves to `#C8642A` per the Tailwind tokens established in Phase 1. This matches the terracotta accent used on the View All link in FeaturedProducts and badge colors in the header.

After editing, confirm:
- No `text-primary-*` class references remain in the file
- No `bg-primary-*` class references remain in the file
- The heading `<h1>` renders `{heading}` directly as a text node (no `.split` or `.map`)
  </action>
  <verify>
    <automated>grep -n "primary" /Users/jamesputman/SRC/shopSite/components/homepage/enhanced-hero.tsx && echo "FAIL: primary classes remain" || echo "PASS: no primary classes found"</automated>
    <automated>grep -n "bg-terracotta" /Users/jamesputman/SRC/shopSite/components/homepage/enhanced-hero.tsx</automated>
    <automated>grep -n "split\|\.map" /Users/jamesputman/SRC/shopSite/components/homepage/enhanced-hero.tsx && echo "FAIL: word-splitting logic remains" || echo "PASS: no split/map in heading"</automated>
  </verify>
  <done>
    - `grep primary components/homepage/enhanced-hero.tsx` returns no matches
    - `grep bg-terracotta components/homepage/enhanced-hero.tsx` returns line ~100 with the CTA class
    - The `<h1>` block is a flat `{heading}` render with no `.split(' ').map(...)` logic
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Visual verification: no purple remains in hero heading or CTA button</name>
  <action>
Human visual review of dev server after Task 1 automated fixes.
Start dev server if not running: `npm run dev` in /Users/jamesputman/SRC/shopSite, then open http://localhost:3000.

What was built (Task 1):
- Removed word-splitting `.split(' ').map(...)` logic from the EnhancedHero `<h1>` — heading now renders flat
- Changed primary CTA button from `bg-primary-600 hover:bg-primary-500` (Cosmic Purple) to `bg-terracotta hover:bg-terracotta/90`
  </action>
  <verify>
    <manual>
1. HERO HEADING: Read the heading text aloud — it should read "Made by hand. Found by heart." in solid white throughout. Inspect every word: "Made", "by", "hand.", "Found", "by", "heart." — none should have any purple, violet, or lavender tint.

2. HERO CTA BUTTON: The primary button ("Wander the Shop") should be a warm rust/terracotta color (#C8642A). It should NOT be purple or violet. Hover over it — it should darken slightly (terracotta/90), not change hue.

3. SECONDARY CTA: The secondary button ("Our Story" or similar) should still be white-bordered/white-text — unchanged.

4. CATEGORY HOVER (regression check): Hover over one of the "Find Your Wild" category cards to confirm the gold border/glow is still intact.
    </manual>
  </verify>
  <done>User types "approved" — hero heading is all-white with no purple words, CTA button is terracotta, Phase 3 HOME-02 gap is closed.</done>
  <resume-signal>Type "approved" if no purple is visible in the heading or CTA button. Describe any remaining purple if it persists.</resume-signal>
</task>

</tasks>

<verification>
The gap is closed when:
1. `enhanced-hero.tsx` has no `text-primary-*` or `bg-primary-*` class references
2. The `<h1>` renders the `heading` prop as a flat text node
3. The primary CTA ternary resolves to `bg-terracotta text-white hover:bg-terracotta/90`
4. Human visual confirmation: no purple in hero heading or CTA button at http://localhost:3000
</verification>

<success_criteria>
- `grep primary components/homepage/enhanced-hero.tsx` returns empty
- `grep bg-terracotta components/homepage/enhanced-hero.tsx` returns one match on the CTA button line
- User types "approved" confirming heading is all-white and CTA is terracotta
- Phase 3 gap documented in VERIFICATION.md is fully closed
</success_criteria>

<output>
After user approval, create `.planning/phases/03-homepage/03-04-SUMMARY.md` recording:
- The two specific edits made (heading flat render + terracotta CTA)
- The line numbers changed (72-82 → flat h1, line 100 → bg-terracotta)
- User approval signal and date
- Confirmation that Phase 3 HOME-02 gap is now fully closed
</output>
