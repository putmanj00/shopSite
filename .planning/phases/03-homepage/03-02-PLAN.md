---
phase: 03-homepage
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - components/homepage/category-cards.tsx
autonomous: true
requirements:
  - HOME-03

must_haves:
  truths:
    - "Category section heading reads 'Find Your Wild' in forest green — no psychedelic copy visible"
    - "Category section background is parchment, not white"
    - "All four category card descriptions use warm Wildenflower botanical voice — no 'psychedelic', 'groovy', or 'trippy' language remains"
    - "All four category images show botanical assets from /assets/images/ — no old /images/ paths or Unsplash URLs"
    - "Category card hover states flash gold, not purple"
  artifacts:
    - path: "components/homepage/category-cards.tsx"
      provides: "Wildenflower-voiced category section with botanical images and palette"
      contains: "Find Your Wild"
  key_links:
    - from: "components/homepage/category-cards.tsx"
      to: "public/assets/images/"
      via: "image paths in categories array"
      pattern: "/assets/images/"
---

<objective>
Rewrite the CategoryCards component with Wildenflower botanical copy, palette colors, and local botanical images — replacing all psychedelic language and purple hover states.

Purpose: The category section is the loudest voice of the old brand identity (purple glows, "Psychedelic swirls and festival-ready vibes"). This plan silences it and replaces it with warm botanical Wildenflower voice.
Output: Updated components/homepage/category-cards.tsx with Wildenflower section copy, card descriptions, botanical image paths, and gold hover states.
</objective>

<execution_context>
@/Users/jamesputman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesputman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-homepage/03-CONTEXT.md
@.planning/phases/03-homepage/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CategoryCards — botanical copy, palette colors, images, and hover states</name>
  <files>components/homepage/category-cards.tsx</files>
  <action>
Read components/homepage/category-cards.tsx in full first. Then make the following targeted changes:

1. SECTION HEADING (HOME-03) — locked decision
   - Find heading text "Trippy Treasures Await" (or similar psychedelic heading)
   - Replace with: "Find Your Wild"
   - Update heading classes: `text-neutral-900` → `text-forest`, add `font-heading` if not present

2. SECTION SUBHEADING (HOME-03) — Claude's discretion
   - Find subheading "Explore our groovy collections of handcrafted wearable art" (or similar)
   - Replace with warm Wildenflower botanical copy. Example tone: "Each collection is a gathering — wild-crafted things made slowly and meant to last." Keep 1 sentence, unhurried.
   - Update subheading classes: `text-neutral-600` → `text-ink-brown`

3. SECTION BACKGROUND (HOME-03)
   - Find section wrapper with `bg-white` (or `bg-neutral-*`)
   - Replace with `bg-parchment` (Tailwind 4 token — confirmed working in Phase 2 header)

4. STATIC CATEGORIES ARRAY (HOME-03) — update all four entries
   Update the hardcoded `categories` array. Change ONLY `description` and `image` fields for each entry. Do NOT change `handle`, `title`, `productCount`, or `href`.

   Recommended image assignments (use local /assets/images/ paths — do NOT use Unsplash or /images/ paths):
   - Tie-Dye category: image → `/assets/images/headers/botanical-header-small.png`
   - Mandala Art category: image → `/assets/images/headers/botanical-header-large1.png`
   - Jewelry category: image → `/assets/images/splash/splash-bloom-elements.png`
   - Crystals category: image → `/assets/images/headers/botanical-header-large.png`

   If the category titles are different from above, assign botanical assets using best judgment — any /assets/images/ path is preferable to /images/ or Unsplash URLs.

   Description rewrites — Claude's discretion. Write in warm Wildenflower voice for each category. Examples of appropriate tone:
   - Tie-Dye: "Hand-dyed in small batches. Each piece blooms differently."
   - Mandala Art: "Drawn by hand, one patient line at a time."
   - Jewelry: "Foraged shapes, gathered light."
   - Crystals: "Earth-kept for years. Yours now."
   Keep each description short (1–2 short sentences), no psychedelic language.

5. HOVER STATE COLORS (HOME-03)
   - Find all instances of `text-primary-300` in the file → replace with `text-gold`
   - Find all instances of `border-primary-400` or `border-primary-400/50` → replace with `border-gold/50`
   - These appear on card hover animation elements and may only be visible on hover

6. CTA LINK COLOR (HOME-03)
   - Find "Explore" or "Shop Now" CTA link color on cards, likely `text-primary-*`
   - Replace with `text-gold`

Do NOT change: card grid structure (4 columns), aspect ratios, hover scale/shadow effects, or any JSX structure.
  </action>
  <verify>
    <automated>cd /Users/jamesputman/SRC/shopSite && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>In dev server at http://localhost:3000, scroll to category section. Confirm: (1) parchment background not white, (2) heading "Find Your Wild" in green, (3) no psychedelic description text, (4) botanical images in cards, (5) hover effect shows gold not purple glow.</manual>
    <sampling_rate>run after this task completes</sampling_rate>
  </verify>
  <done>
    - Section heading is "Find Your Wild" with `text-forest font-heading`
    - Section background is `bg-parchment` not `bg-white`
    - All four category `image` values are `/assets/images/` paths (no /images/ or Unsplash)
    - All four category `description` values use warm Wildenflower voice — no psychedelic language
    - No `text-primary-*` or `border-primary-*` classes remain in the file
    - TypeScript compiles clean
  </done>
</task>

</tasks>

<verification>
Run `npx tsc --noEmit` from project root — must produce no errors.
Grep confirms: `grep -n "Find Your Wild" components/homepage/category-cards.tsx` finds the heading.
Grep confirms: `grep -n "bg-parchment" components/homepage/category-cards.tsx` finds section wrapper.
Grep confirms: `grep -c "/assets/images/" components/homepage/category-cards.tsx` returns 4 or more (one per category).
Grep confirms: `grep -c "primary" components/homepage/category-cards.tsx` returns 0 (no purple color refs remain).
Grep confirms: `grep -c "groovy\|trippy\|psychedelic" components/homepage/category-cards.tsx` returns 0.
</verification>

<success_criteria>
- Category section heading is "Find Your Wild" in forest green on parchment background
- All four category card descriptions written in warm Wildenflower voice — no psychedelic language
- All four category images use /assets/images/ local paths — no /images/ or Unsplash URLs
- Hover states show gold accent not purple — no primary-* color classes in file
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-homepage/03-02-SUMMARY.md` following the summary template.
</output>
