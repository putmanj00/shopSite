---
phase: 05-supporting-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ui/botanical-header.tsx
  - app/about/page.tsx
autonomous: true
requirements:
  - SUPP-01

must_haves:
  truths:
    - "BotanicalHeader component supports an 'about' variant that maps to botanical-header-large-about.png"
    - "About page renders BotanicalHeader (about variant) as the first element before AboutHero"
    - "divider-fallen-log.png is visible between MissionValues and Sustainability sections"
    - "All existing About page sections remain present and in original order"
  artifacts:
    - path: "components/ui/botanical-header.tsx"
      provides: "New 'about' variant mapping"
      contains: "botanical-header-large-about"
    - path: "app/about/page.tsx"
      provides: "BotanicalHeader + fallen-log divider insertion"
      contains: "BotanicalHeader"
  key_links:
    - from: "app/about/page.tsx"
      to: "components/ui/botanical-header.tsx"
      via: "import + JSX element"
      pattern: "BotanicalHeader.*variant.*about"
---

<objective>
Add BotanicalHeader (about variant) to the top of the About page and insert divider-fallen-log.png between the MissionValues and Sustainability sections.

Purpose: SUPP-01 — About page shows botanical-header-large-about.png at top; divider-fallen-log.png is visible within the existing layout.
Output: BotanicalHeader component extended with `about` variant; about/page.tsx with header at top and fallen-log divider between sections.
</objective>

<execution_context>
@/Users/jamesputman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesputman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-supporting-pages/05-CONTEXT.md
@.planning/phases/05-supporting-pages/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 'about' variant to BotanicalHeader</name>
  <files>components/ui/botanical-header.tsx</files>
  <action>
Read the full file first. The component has a `variant` prop union type and two lookup objects: `HEADER_ASSETS` and `ASPECT_RATIOS`.

1. Add `'about'` to the variant union type: `'large' | 'small' | 'faq' | 'blog' | 'about'`

2. Add the about entry to `HEADER_ASSETS`:
   ```tsx
   about: '/assets/images/headers/botanical-header-large-about.png',
   ```

3. Add the about entry to `ASPECT_RATIOS`. The image is `botanical-header-large-about.png` — use the same aspect ratio as `large` (1408/768) since both are "large" format headers:
   ```tsx
   about: 1408 / 768,
   ```

Anti-patterns:
- Do NOT change any existing variants or their mappings
- Do NOT alter the rendering logic
  </action>
  <verify>
    <automated>cd /Users/jamesputman/SRC/shopSite && grep -n "about" components/ui/botanical-header.tsx</automated>
    <manual>'about' appears in the variant type, HEADER_ASSETS, and ASPECT_RATIOS</manual>
  </verify>
  <done>BotanicalHeader component supports 'about' variant mapping to botanical-header-large-about.png</done>
</task>

<task type="auto">
  <name>Task 2: Insert BotanicalHeader and fallen-log divider into About page</name>
  <files>app/about/page.tsx</files>
  <action>
Read the full file first. The page is a Server Component rendering 8 section components in a fragment.

1. Add imports at the top of the file:
   ```tsx
   import { BotanicalHeader } from '@/components/ui/botanical-header';
   import Image from 'next/image';
   ```

2. In the return JSX, insert `<BotanicalHeader variant="about" />` as the FIRST element inside the fragment, before `<AboutHero />`.

3. Between `<MissionValues />` and `<Sustainability />`, insert a fallen-log divider using a standalone Image — BotanicalDivider component does not have a fallen-log variant and adding one would be scope creep. Use the same visual pattern as BotanicalDivider:
   ```tsx
   {/* Fallen Log Divider */}
   <div className="w-full h-32 my-6 flex items-center justify-center mx-auto max-w-[1500px] relative">
     <Image
       src="/assets/images/about/divider-fallen-log.png"
       alt="Botanical divider - fallen log"
       fill
       sizes="(max-width: 1500px) 100vw, 1500px"
       className="object-contain"
     />
   </div>
   ```

Anti-patterns:
- Do NOT remove or reorder any existing section components
- Do NOT change the metadata export
- Do NOT add className or styling to the fragment wrapper
  </action>
  <verify>
    <automated>cd /Users/jamesputman/SRC/shopSite && grep -n "BotanicalHeader\|fallen-log\|divider" app/about/page.tsx</automated>
    <manual>BotanicalHeader renders above AboutHero; fallen-log divider renders between MissionValues and Sustainability</manual>
  </verify>
  <done>About page has BotanicalHeader (about variant) at top and fallen-log divider between MissionValues and Sustainability; all 8 original sections preserved</done>
</task>

</tasks>

<verification>
Run TypeScript check to confirm no import errors:
```bash
cd /Users/jamesputman/SRC/shopSite && npx tsc --noEmit 2>&1 | grep -E "about|botanical-header" | head -20
```
Expected: no errors.

Confirm asset resolves:
```bash
ls /Users/jamesputman/SRC/shopSite/public/assets/images/headers/botanical-header-large-about.png && ls /Users/jamesputman/SRC/shopSite/public/assets/images/about/divider-fallen-log.png
```
Expected: both files exist.
</verification>

<success_criteria>
- `components/ui/botanical-header.tsx` has `about` in variant type union, HEADER_ASSETS, and ASPECT_RATIOS
- `app/about/page.tsx` imports and renders `<BotanicalHeader variant="about" />` before `<AboutHero />`
- `app/about/page.tsx` renders fallen-log divider image between `<MissionValues />` and `<Sustainability />`
- All 8 original sections remain present and in original order
- TypeScript compiles with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-supporting-pages/05-01-SUMMARY.md` following the summary template.
</output>
