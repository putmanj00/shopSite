---
phase: 05-supporting-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/faq/page.tsx
  - data/faq-data.ts
  - components/faq/faq-accordion.tsx
autonomous: true
requirements:
  - SUPP-02

must_haves:
  truths:
    - "FAQ page renders BotanicalHeader (faq variant) at the top"
    - "ComingSoon placeholder is completely replaced with a real FAQ page"
    - "FAQ accordion displays 7 items sourced from data/faq-data.ts"
    - "Category filter chips (All / Getting Started / Shipping / Makers / Returns) filter the accordion list"
    - "Accordion expand/collapse icons use fern-expand.png and fern-collapse.png"
    - "'Still curious?' contact section renders at the bottom with faq-contact-border.png"
  artifacts:
    - path: "app/faq/page.tsx"
      provides: "Full FAQ page with BotanicalHeader, title, filter chips, accordion, contact section"
      contains: "BotanicalHeader"
    - path: "data/faq-data.ts"
      provides: "7 FAQ items with categories"
      contains: "faqItems"
    - path: "components/faq/faq-accordion.tsx"
      provides: "Accordion component with fern icons"
      contains: "fern-expand"
  key_links:
    - from: "app/faq/page.tsx"
      to: "components/ui/botanical-header.tsx"
      via: "import + JSX element"
      pattern: "BotanicalHeader.*variant.*faq"
    - from: "app/faq/page.tsx"
      to: "components/faq/faq-accordion.tsx"
      via: "import + JSX element"
      pattern: "FaqAccordion"
    - from: "components/faq/faq-accordion.tsx"
      to: "data/faq-data.ts"
      via: "import faqItems"
      pattern: "faqItems"
---

<objective>
Build a full FAQ page replacing the ComingSoon placeholder. Add BotanicalHeader (faq variant), page title with subtitle, category filter chips, a 7-item accordion using fern icons, and a "Still curious?" contact section with faq-contact-border.png.

Purpose: SUPP-02 — FAQ page shows botanical-header-faq.png at top; accordion icons use fern-expand/collapse.png.
Output: Three new/modified files — faq data file, accordion component, and rewritten page.
</objective>

<execution_context>
@/Users/jamesputman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesputman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-supporting-pages/05-CONTEXT.md
@.planning/phases/05-supporting-pages/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FAQ data file</name>
  <files>data/faq-data.ts</files>
  <action>
Create a new file at `data/faq-data.ts`. This file provides the FAQ content sourced from the wildenflowerShop mock data (7 items, 4 categories).

Define the types and data:

```tsx
export interface FaqItem {
  id: string;
  question: string;
  answer: string;
  category: string;
}

export const FAQ_CATEGORIES = ['All', 'Getting Started', 'Shipping', 'Makers', 'Returns'] as const;

export const faqItems: FaqItem[] = [
  {
    id: '1',
    question: 'What makes Wildenflower products unique?',
    answer: 'Every item in our shop is handcrafted by independent artisans who pour their hearts into their work. We personally vet each maker to ensure quality, authenticity, and ethical practices. No mass production — just genuine craftsmanship.',
    category: 'Getting Started',
  },
  {
    id: '2',
    question: 'How do I know which size to choose?',
    answer: 'Each product listing includes detailed measurements and a size guide. If you are between sizes, we recommend sizing up for a comfortable fit. You can also reach out to us directly and we will help you find the perfect fit.',
    category: 'Getting Started',
  },
  {
    id: '3',
    question: 'How long does shipping take?',
    answer: 'Standard shipping takes 5–7 business days within the US. Express shipping (2–3 business days) is available at checkout. International orders typically arrive within 10–14 business days. Because many items are made to order, please allow an additional 3–5 days for crafting.',
    category: 'Shipping',
  },
  {
    id: '4',
    question: 'Do you ship internationally?',
    answer: 'Yes! We ship to over 40 countries. International shipping rates and delivery times vary by destination. Customs duties and taxes may apply and are the responsibility of the buyer.',
    category: 'Shipping',
  },
  {
    id: '5',
    question: 'Who are your makers?',
    answer: 'Our makers are independent artisans from around the world — ceramicists, weavers, woodworkers, jewelers, and more. We build long-term relationships with each maker and ensure they receive fair compensation. You can learn more about individual makers on each product page.',
    category: 'Makers',
  },
  {
    id: '6',
    question: 'What is your return policy?',
    answer: 'We offer a 30-day return policy for unused items in their original packaging. Handmade items may have slight variations that are part of their charm and are not considered defects. If you receive a damaged item, contact us within 48 hours for a full refund or replacement.',
    category: 'Returns',
  },
  {
    id: '7',
    question: 'Can I exchange an item?',
    answer: 'Absolutely! We are happy to help with exchanges. Simply initiate a return and place a new order for the item you would like. If you need help choosing, our team is always available to assist.',
    category: 'Returns',
  },
];
```

This is a new file — no existing content to preserve.
  </action>
  <verify>
    <automated>cd /Users/jamesputman/SRC/shopSite && cat data/faq-data.ts | grep -c "id:" </automated>
    <manual>File contains 7 FAQ items and the FAQ_CATEGORIES array</manual>
  </verify>
  <done>data/faq-data.ts created with FaqItem type, FAQ_CATEGORIES, and 7 faqItems</done>
</task>

<task type="auto">
  <name>Task 2: Create FAQ accordion component</name>
  <files>components/faq/faq-accordion.tsx</files>
  <action>
Create a new Client Component at `components/faq/faq-accordion.tsx`.

The component should:

1. Accept props: `items: FaqItem[]` (already filtered by parent)
2. Manage `openId: string | null` state for single-open accordion behavior
3. Render a list of accordion rows, each with:
   - A button containing the question text and a fern icon
   - When collapsed: show `fern-expand.png` icon
   - When expanded: show `fern-collapse.png` icon
   - Icons at `w-6 h-6` using `next/image`
4. Expanded content slides open with CSS transition (max-height transition)
5. Styling per 05-CONTEXT.md (Claude's discretion):
   - Each row: `bg-white rounded-lg shadow-sm mb-3` (white card on parchment pattern)
   - Question text: `text-ink-brown font-medium`
   - Answer text: `text-earth leading-relaxed`
   - Button padding: `p-5`
   - Content padding: `px-5 pb-5`

```tsx
'use client';

import { useState } from 'react';
import Image from 'next/image';
import type { FaqItem } from '@/data/faq-data';

interface FaqAccordionProps {
  items: FaqItem[];
}

export function FaqAccordion({ items }: FaqAccordionProps) {
  const [openId, setOpenId] = useState<string | null>(null);

  const toggle = (id: string) => {
    setOpenId((prev) => (prev === id ? null : id));
  };

  return (
    <div className="space-y-3">
      {items.map((item) => {
        const isOpen = openId === item.id;
        return (
          <div key={item.id} className="bg-white rounded-lg shadow-sm overflow-hidden">
            <button
              onClick={() => toggle(item.id)}
              className="w-full flex items-center justify-between p-5 text-left"
              aria-expanded={isOpen}
            >
              <span className="text-ink-brown font-medium pr-4">{item.question}</span>
              <Image
                src={isOpen
                  ? '/assets/images/icons/ui/fern-collapse.png'
                  : '/assets/images/icons/ui/fern-expand.png'}
                alt={isOpen ? 'Collapse' : 'Expand'}
                width={24}
                height={24}
                className="flex-shrink-0"
              />
            </button>
            <div
              className="transition-all duration-300 ease-in-out overflow-hidden"
              style={{ maxHeight: isOpen ? '500px' : '0' }}
            >
              <div className="px-5 pb-5">
                <p className="text-earth leading-relaxed">{item.answer}</p>
              </div>
            </div>
          </div>
        );
      })}
    </div>
  );
}
```

This is a new file — no existing content to preserve.
  </action>
  <verify>
    <automated>cd /Users/jamesputman/SRC/shopSite && grep -n "fern-expand\|fern-collapse" components/faq/faq-accordion.tsx</automated>
    <manual>Component imports fern icons, renders expand/collapse based on open state</manual>
  </verify>
  <done>components/faq/faq-accordion.tsx created with single-open accordion, fern icons, white-card styling, CSS max-height transition</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite FAQ page</name>
  <files>app/faq/page.tsx</files>
  <action>
Replace the entire FAQ page. Remove the ComingSoon import and replace with a full FAQ page.

The page is a mix of Server and Client concerns:
- Metadata export stays as Server Component (no `'use client'`)
- Create a Client Component wrapper for the interactive parts, OR use the accordion as the only client component and keep the page as Server Component

Structure (Server Component page):

```tsx
import type { Metadata } from 'next';
import { BotanicalHeader } from '@/components/ui/botanical-header';
import { FaqAccordion } from '@/components/faq/faq-accordion';
import { faqItems, FAQ_CATEGORIES } from '@/data/faq-data';
import { FaqPageContent } from '@/components/faq/faq-page-content';
import Image from 'next/image';

export const metadata: Metadata = {
  title: 'FAQ | Wildenflower',
  description: 'Everything you might want to know about Wildenflower — from our handmade products to shipping and returns.',
};

export default function FAQPage() {
  return (
    <>
      <BotanicalHeader variant="faq" />
      <FaqPageContent />
    </>
  );
}
```

Because the filter chips need state (active category), create a new Client Component:

**Create `components/faq/faq-page-content.tsx`:**

```tsx
'use client';

import { useState } from 'react';
import Image from 'next/image';
import { FaqAccordion } from '@/components/faq/faq-accordion';
import { faqItems, FAQ_CATEGORIES } from '@/data/faq-data';

export function FaqPageContent() {
  const [activeCategory, setActiveCategory] = useState<string>('All');

  const filteredItems = activeCategory === 'All'
    ? faqItems
    : faqItems.filter((item) => item.category === activeCategory);

  return (
    <div className="bg-parchment min-h-screen">
      <div className="container mx-auto px-4 py-12 max-w-3xl">
        {/* Page Title */}
        <div className="text-center mb-10">
          <h1 className="text-4xl font-bold text-ink-brown mb-3">Questions</h1>
          <p className="text-lg text-earth">Everything you might want to know about Wildenflower</p>
        </div>

        {/* Category Filter Chips */}
        <div className="flex flex-wrap justify-center gap-2 mb-10">
          {FAQ_CATEGORIES.map((category) => (
            <button
              key={category}
              onClick={() => setActiveCategory(category)}
              className={`px-4 py-2 rounded-full text-sm font-medium transition-colors ${
                activeCategory === category
                  ? 'bg-forest text-parchment'
                  : 'bg-white text-ink-brown border border-gold/30 hover:border-forest hover:text-forest'
              }`}
            >
              {category}
            </button>
          ))}
        </div>

        {/* Accordion */}
        <FaqAccordion items={filteredItems} />

        {/* Still Curious? Contact Section */}
        <div className="mt-16 relative">
          <div className="relative w-full" style={{ aspectRatio: '1170/400' }}>
            <Image
              src="/assets/images/faq/faq-contact-border.png"
              alt="Decorative botanical border"
              fill
              sizes="(max-width: 768px) 100vw, 768px"
              className="object-contain"
            />
          </div>
          <div className="text-center -mt-8 relative z-10 pb-4">
            <h2 className="text-2xl font-bold text-ink-brown mb-2">Still curious?</h2>
            <p className="text-earth mb-4">We'd love to hear from you</p>
            <a
              href="mailto:wildenflowercreations@gmail.com"
              className="inline-block px-6 py-3 bg-terracotta text-white rounded-lg font-medium hover:bg-terracotta/90 transition-colors"
            >
              Get in Touch
            </a>
          </div>
        </div>
      </div>
    </div>
  );
}
```

Anti-patterns:
- Do NOT keep the ComingSoon component import
- Do NOT use `'use client'` on the page.tsx itself (keep metadata export working)
  </action>
  <verify>
    <automated>cd /Users/jamesputman/SRC/shopSite && grep -n "BotanicalHeader\|FaqPageContent\|ComingSoon" app/faq/page.tsx</automated>
    <manual>Page renders BotanicalHeader + FaqPageContent; no ComingSoon reference; filter chips and accordion visible</manual>
  </verify>
  <done>FAQ page completely rewritten: BotanicalHeader (faq variant) at top, "Questions" heading + subtitle, 5 category filter chips with active state, 7-item accordion with fern icons, "Still curious?" contact section with faq-contact-border.png</done>
</task>

</tasks>

<verification>
Run TypeScript check to confirm no import errors:
```bash
cd /Users/jamesputman/SRC/shopSite && npx tsc --noEmit 2>&1 | grep -E "faq|FaqAccordion|faq-data" | head -20
```
Expected: no errors.

Confirm assets resolve:
```bash
ls /Users/jamesputman/SRC/shopSite/public/assets/images/headers/botanical-header-faq.png && ls /Users/jamesputman/SRC/shopSite/public/assets/images/icons/ui/fern-expand.png && ls /Users/jamesputman/SRC/shopSite/public/assets/images/icons/ui/fern-collapse.png && ls /Users/jamesputman/SRC/shopSite/public/assets/images/faq/faq-contact-border.png
```
Expected: all files exist.

Confirm ComingSoon is fully removed:
```bash
grep -rn "ComingSoon" /Users/jamesputman/SRC/shopSite/app/faq/
```
Expected: empty output.
</verification>

<success_criteria>
- `data/faq-data.ts` exports `FaqItem` type, `FAQ_CATEGORIES`, and `faqItems` (7 items)
- `components/faq/faq-accordion.tsx` renders items with fern-expand.png / fern-collapse.png toggle icons
- `components/faq/faq-page-content.tsx` has category filter chips with active state and renders FaqAccordion with filtered items
- `app/faq/page.tsx` renders `<BotanicalHeader variant="faq" />` + `<FaqPageContent />`
- No `ComingSoon` references remain in `app/faq/`
- "Still curious?" section renders with faq-contact-border.png
- TypeScript compiles clean across all new/modified files
</success_criteria>

<output>
After completion, create `.planning/phases/05-supporting-pages/05-02-SUMMARY.md` following the summary template.
</output>
