---
phase: 05-supporting-pages
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/blog/page.tsx
autonomous: true
requirements:
  - SUPP-03

must_haves:
  truths:
    - "Blog page renders BotanicalHeader (blog variant) as the first element above the ComingSoon placeholder"
    - "ComingSoon placeholder is preserved with its existing title and description"
    - "No other changes to the Blog page"
  artifacts:
    - path: "app/blog/page.tsx"
      provides: "BotanicalHeader insertion above ComingSoon"
      contains: "BotanicalHeader"
  key_links:
    - from: "app/blog/page.tsx"
      to: "components/ui/botanical-header.tsx"
      via: "import + JSX element"
      pattern: "BotanicalHeader.*variant.*blog"
---

<objective>
Add BotanicalHeader (blog variant) to the top of the Blog page, above the existing ComingSoon placeholder.

Purpose: SUPP-03 — Blog/Field Notes page shows botanical-header-blog.png at top.
Output: blog/page.tsx with BotanicalHeader added; ComingSoon preserved.
</objective>

<execution_context>
@/Users/jamesputman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesputman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-supporting-pages/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert BotanicalHeader above ComingSoon on Blog page</name>
  <files>app/blog/page.tsx</files>
  <action>
Read the full file first. The page currently imports ComingSoon and renders it directly in the return.

1. Add import at the top of the file:
   ```tsx
   import { BotanicalHeader } from '@/components/ui/botanical-header';
   ```

2. Wrap the return in a fragment and insert `<BotanicalHeader variant="blog" />` before `<ComingSoon />`:
   ```tsx
   export default function BlogPage() {
     return (
       <>
         <BotanicalHeader variant="blog" />
         <ComingSoon title="Journal" description="Our blog features stories from our artisans and lifestyle inspiration. First post coming soon!" />
       </>
     );
   }
   ```

Anti-patterns:
- Do NOT remove or modify the ComingSoon component or its props
- Do NOT change the metadata export
  </action>
  <verify>
    <automated>cd /Users/jamesputman/SRC/shopSite && grep -n "BotanicalHeader\|ComingSoon" app/blog/page.tsx</automated>
    <manual>BotanicalHeader renders above ComingSoon; both are present</manual>
  </verify>
  <done>Blog page renders BotanicalHeader (blog variant) above the preserved ComingSoon placeholder</done>
</task>

</tasks>

<verification>
Run TypeScript check to confirm no import errors:
```bash
cd /Users/jamesputman/SRC/shopSite && npx tsc --noEmit 2>&1 | grep -E "blog" | head -10
```
Expected: no errors.

Confirm asset resolves:
```bash
ls /Users/jamesputman/SRC/shopSite/public/assets/images/headers/botanical-header-blog.png
```
Expected: file exists.
</verification>

<success_criteria>
- `app/blog/page.tsx` imports `BotanicalHeader` and renders `<BotanicalHeader variant="blog" />` before `<ComingSoon />`
- ComingSoon component and its props are unchanged
- TypeScript compiles with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-supporting-pages/05-03-SUMMARY.md` following the summary template.
</output>
