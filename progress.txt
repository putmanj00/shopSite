# Ralph Progress Log
Started: Sun Jan 12 2026

## Codebase Patterns

- **Next.js Setup**: Using Next.js 14+ with App Router (not Pages Router)
- **TypeScript**: Strict mode enabled for maximum type safety
- **Styling**: Tailwind CSS v4 with PostCSS for modern utility-first styling
- **Code Quality**: ESLint + Prettier configured for consistent code style
- **Import Aliases**: Using `@/*` for clean imports from project root
- **Quality Scripts**: `npm run typecheck`, `npm run lint`, `npm run build` must all pass before committing
- **Shopify API**: Use lazy-loaded client pattern (lib/shopify.ts) to avoid build-time errors with environment variables
- **GraphQL**: Queries stored in lib/shopify-queries.ts with reusable fragments for maintainability
- **API Helpers**: Use helper functions from lib/shopify-helpers.ts for all Shopify operations (includes error handling)
- **React Components**: No JSX in try/catch blocks (ESLint rule) - separate data fetching from rendering
- **Server Components**: Use async server components for data fetching, wrap in Suspense for loading states
- **Responsive Design**: Mobile-first with Tailwind breakpoints (sm, md, lg, xl)
- **URL State Management**: Use Next.js useRouter and useSearchParams for persisting filter/sort state in URL
- **Client Components**: Use 'use client' directive for components with interactivity (useState, useEffect, event handlers)
- **Debouncing**: Use useRef with setTimeout for debounced inputs (e.g., search bars)
- **React ESLint Rules**: Avoid setState in useEffect - use component keys to reset state or initialize from props

---

## Progress Entries

## 2026-01-12 - Story #1: Project Foundation Setup

**Thread**: N/A (First iteration)

**Implementation**:
- Initialized Next.js 14+ project (v16.1.1) with App Router using create-next-app
- TypeScript already configured with strict mode by default
- Tailwind CSS v4 set up with PostCSS configuration
- ESLint configured with Next.js recommended rules (using new flat config format)
- Added Prettier with ESLint integration for code formatting
- Created folder structure: components/, lib/, types/ (app/ created by Next.js)
- Environment variables template (.env.example) was already present from project setup
- Added npm scripts for typecheck, format, and format:check

**Files Changed**:
- package.json - Added Prettier dependencies and quality check scripts
- .prettierrc - Prettier configuration with project standards
- .prettierignore - Ignore build artifacts and dependencies
- tsconfig.json - Strict TypeScript configuration (created by Next.js)
- eslint.config.mjs - ESLint configuration (created by Next.js)
- components/.gitkeep, lib/.gitkeep, types/.gitkeep - Track empty directories
- app/* - Next.js default app structure with layout and page

**Learnings for Future Iterations**:
- Next.js 14+ uses new flat ESLint config format (eslint.config.mjs, not .eslintrc.json)
- Tailwind CSS v4 uses @tailwindcss/postcss instead of traditional PostCSS plugins
- The project uses npm (not yarn/pnpm) - continue with npm for consistency
- Quality checks (typecheck, lint, build) should all pass before any commit
- Import alias `@/*` is configured and should be used for cleaner imports
- Directory name had capital letters which caused initial create-next-app issues

---

## 2026-01-12 - Story #2: Shopify Storefront API Integration

**Thread**: Autonomous iteration #2

**Implementation**:
- Installed @shopify/storefront-api-client (official Shopify library) and GraphQL
- Created lazy-loaded Shopify API client to avoid build-time environment variable errors
- Implemented comprehensive TypeScript type definitions for all Shopify models (Product, Collection, Cart, Customer, etc.)
- Set up GraphQL queries with reusable fragments for products, collections, and search
- Created helper functions (getProducts, getProductByHandle, getCollections, getCollectionByHandle) with robust error handling
- Added utility functions for formatting money and checking product sale status
- Created test API route at /api/test-shopify to verify Shopify connection

**Files Changed**:
- package.json, package-lock.json - Added @shopify/storefront-api-client and graphql dependencies
- lib/shopify.ts - Lazy-loaded Shopify client with environment validation
- lib/shopify-queries.ts - GraphQL queries with fragments for products and collections
- lib/shopify-helpers.ts - Helper functions with error handling for common operations
- types/shopify.ts - Comprehensive TypeScript type definitions for Shopify data models
- app/api/test-shopify/route.ts - Test endpoint to verify API connection

**Learnings for Future Iterations**:
- **Lazy Loading for Build Success**: Module-level code that requires environment variables will break Next.js builds. Use lazy loading (create client on first use) instead of module-level initialization
- **GraphQL Fragments**: Use fragments to keep queries DRY and maintainable - defined MoneyFragment, ImageFragment, VariantFragment, ProductFragment
- **Error Handling Pattern**: Wrap all Shopify API calls in try/catch, check for response.errors, and provide helpful error messages
- **API Version**: Using Shopify API version '2024-01' - update periodically as Shopify releases new versions
- **Test Route**: The /api/test-shopify endpoint can be used to verify Shopify credentials are configured correctly
- **TypeScript Types**: Created comprehensive types matching Shopify's GraphQL schema for compile-time safety

---

## 2026-01-12 - Story #3: Homepage with Featured Products

**Thread**: Autonomous iteration #3

**Implementation**:
- Created modern Hero component with gradient background and call-to-action buttons
- Built ProductCard component with responsive design, hover effects, and badges for sale/sold-out status
- Implemented FeaturedProducts server component that fetches from Shopify API
- Added comprehensive loading states using Suspense and skeleton components
- Configured SEO metadata in layout.tsx with OpenGraph and Twitter Card support
- Implemented proper error handling by separating data fetching from JSX rendering (to comply with React ESLint rules)
- Responsive grid layout using Tailwind (1 col mobile, 2 cols tablet, 3-4 cols desktop)

**Files Changed**:
- app/page.tsx - Complete homepage redesign with Hero and FeaturedProducts sections
- app/layout.tsx - Enhanced SEO metadata with OpenGraph, Twitter Cards, and robots directives
- app/loading.tsx - Page-level loading skeleton for better UX
- components/hero.tsx - Hero section with gradient design and CTAs
- components/product-card.tsx - Product card with image, title, price, badges, hover effects
- components/product-card-skeleton.tsx - Loading skeleton for product cards
- components/featured-products.tsx - Server component fetching and displaying featured products

**Learnings for Future Iterations**:
- **No JSX in Try/Catch**: ESLint rule `react-hooks/error-boundaries` prevents JSX construction inside try/catch blocks. Solution: Separate data fetching (in try/catch) from JSX rendering, or create separate component functions for error/empty states
- **Server Components**: Featured products is an async server component that fetches data at request time. Next.js handles this seamlessly with Suspense boundaries
- **Skeleton Loading**: Use skeleton components with `animate-pulse` class for better perceived performance
- **Metadata API**: Next.js 14+ App Router uses export const metadata for SEO - supports templates, OpenGraph, Twitter Cards, and robots
- **Image Optimization**: Using Next.js Image component with `fill` and `object-cover` for responsive product images
- **Tailwind Responsive**: Grid responsive breakpoints - grid-cols-1 (mobile), sm:grid-cols-2 (tablet), lg:grid-cols-3, xl:grid-cols-4 (desktop)

---

## 2026-01-12 - Story #4: Product Collection Pages with Search & Filter

**Thread**: Autonomous iteration #4

**Implementation**:
- Created dynamic collection page route at /collections/[handle] with Next.js App Router
- Implemented CollectionContent client component with comprehensive filtering and sorting logic
- Built SearchBar component with 500ms debounced input using useRef and setTimeout
- Created FilterPanel component for desktop sidebar with collapsible sections (Price, Type, Tags)
- Developed MobileFilterDrawer component with slide-out animation and body scroll lock
- Added SortDropdown with 6 options: Featured, Price (asc/desc), Newest, Name (A-Z/Z-A)
- Implemented pagination with smart ellipsis display (shows first, last, current, and adjacent pages)
- All filter/sort state persisted in URL query parameters for shareability
- Responsive design: Desktop shows sidebar, mobile shows filter button with drawer
- Empty states with clear call-to-action when no products match filters

**Files Changed**:
- app/collections/[handle]/page.tsx - Dynamic collection route with metadata generation and error handling
- components/collection-content.tsx - Main filtering/sorting logic with URL state management
- components/search-bar.tsx - Debounced search input with clear button
- components/filter-panel.tsx - Desktop filter sidebar with collapsible sections
- components/mobile-filter-drawer.tsx - Mobile filter drawer with backdrop and slide animation
- components/sort-dropdown.tsx - Sort dropdown with 6 sorting options

**Learnings for Future Iterations**:
- **URL State Persistence**: Use URLSearchParams with useRouter to persist all UI state in the URL - makes pages shareable and supports browser back/forward
- **Debounced Input**: Pattern is: useRef for timer, useEffect to set/clear timeout, cleanup in return. TypeScript requires `useRef<NodeJS.Timeout | undefined>(undefined)`
- **Client vs Server Components**: Collection page is server component (fetches data), CollectionContent is client component (handles interactivity)
- **React ESLint Strict Rules**: `react-hooks/set-state-in-effect` prevents setState in useEffect. Solutions: (1) Initialize state from props, (2) Use component key to reset, (3) Only setState conditionally based on external event like isOpen
- **Mobile Filter Pattern**: Use fixed positioning with backdrop, prevent body scroll with useEffect, provide "Show Results" button to close
- **Pagination Math**: `Math.ceil(total / perPage)` for total pages, `(page - 1) * perPage` for start index
- **Filter Logic**: Apply filters client-side using useMemo for performance - filter, then sort, then paginate
- **Type Safety**: Pass all searchParams through Promise unwrapping in Next.js 15+ (async params and searchParams)

---
## 2026-01-12 - Story #5: Individual Product Pages

**Thread**: Autonomous iteration #5

**Implementation**:
- Created dynamic product page route at /products/[handle] with Next.js App Router
- Implemented ImageGallery component with click-to-zoom functionality and thumbnail grid navigation
- Built intelligent VariantSelector component that handles multiple option types (size, color, etc.) with availability checking
- Created ProductInfo component displaying price (with sale calculations), stock status with low inventory warnings, and product metadata
- Developed AddToCartButton with loading states and success feedback (placeholder implementation for Story #6)
- Added Breadcrumbs component with smart navigation (Home > Category > Product)
- Implemented RelatedProducts section using product type and tags for intelligent recommendations
- Generated structured data (JSON-LD Product schema) for SEO with proper schema.org markup
- Product page includes rich HTML description rendering with prose styling
- Full responsive design with mobile-first approach

**Files Changed**:
- app/products/[handle]/page.tsx - Dynamic product page with metadata generation, structured data, and layout
- components/image-gallery.tsx - Image gallery with zoom, thumbnails, and image counter
- components/product-info.tsx - Product details, pricing, stock status, and metadata display
- components/variant-selector.tsx - Smart variant selection with availability checking
- components/add-to-cart-button.tsx - Add to cart button with loading/success states
- components/breadcrumbs.tsx - Breadcrumb navigation component
- components/related-products.tsx - Related products fetching and display

**Learnings for Future Iterations**:
- **Image Zoom Pattern**: Use onClick with state toggle and CSS transform scale for simple zoom functionality - no complex libraries needed
- **Variant Selection Logic**: Group variants by option names, track selected options, find matching variants when options change
- **Availability Checking**: For each option value, check if any variant exists with that value and is available for sale
- **React Purity Rules**: Cannot use `Date.now()` in render functions - ESLint `react-hooks/purity` will error. Remove dynamic date generation from structured data or move to server-side only
- **OpenGraph Types**: Next.js Metadata API has specific allowed types for OpenGraph - 'product' is not valid, use default or omit type
- **Placeholder Components**: AddToCartButton is a placeholder showing intended UX - actual cart integration happens in Story #6
- **Related Products Strategy**: Try product type first, then tags, then best sellers as fallback for good recommendations
- **Structured Data**: Use JSON-LD script tag with schema.org Product type - includes name, description, images, brand, offers with price and availability
- **Low Stock Warnings**: Show quantity when available < 10 to create urgency without showing exact inventory

---

